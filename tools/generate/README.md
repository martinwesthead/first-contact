# @gendev/generate

Static generator CLI.

Reads a site definition under `sites/<name>/` and emits HTML/CSS/JS/assets into `apps/public-site/public/<name>/`. Uses `@gendev/framework` to render — same renderer the in-browser builder preview uses.

> Not yet implemented — placeholder for the tool skeleton.
