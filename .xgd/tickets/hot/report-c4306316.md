---
uid: report-c4306316
id: REPORT-463
type: report
title: 'Reconciliation Plan: BUNDLE-2 (REQ-1..REQ-8 — platform foundation through
  builder SPA)'
created_by: xgd
created_at: '2026-06-25T00:26:31.155816+00:00'
updated_at: '2026-06-25T00:57:51.886310+00:00'
completed_at: null
last_field_updated: items
fields:
  report_kind: reconciliation_plan
  subject_uid: bundle-94e1d1b6
  anchor_uid: bundle-94e1d1b6
  items:
  - index: 1
    component: Deploy Infrastructure
    item_type: feature
    story_points: 2
    dependencies: []
    description: pnpm monorepo with two Cloudflare Worker apps (apps/public-site,
      apps/control-app), placeholder package skeletons (framework, site-schema, builder-ui,
      ui-kit, generate), GitHub Actions CI workflow (PR install/build/test plus dry-run
      deploy of both Workers) and deploy workflow (push to xgd-stable triggers wrangler
      deploy --env production for both Workers, serialized via concurrency group).
      All identifiers aligned to '1stcontact' slug (root package name, both wrangler
      worker names, sites/ subdirectory, CLAUDE.md heading). control-app Worker returns
      the text/plain 'Hello from app.1stcontact.io' placeholder at /. Node 20+, pnpm
      9+ via corepack; pnpm-lock.yaml committed for --frozen-lockfile reproducibility.
    justification: 'No existing story covers the monorepo scaffold, Worker deploy
      pipeline, or slug-alignment configuration. This is the platform''s deployment
      substrate — every subsequent capability depends on it. Slug rename (REQ-2) is
      configuration alignment within the same deploy-infrastructure story, not a separate
      user-visible capability. Note: the public-site placeholder behavior originally
      shipped in REQ-1 was superseded by REQ-6 (the test was deleted at REQ-6) and
      is therefore deliberately omitted from this story''s surface; current public-site
      behavior is documented in item 7.'
    story_uid: story-067dc2f8
  - index: 2
    component: Site Schema
    item_type: feature
    story_points: 2
    dependencies:
    - 1
    description: '@1stcontact/site-schema package providing Zod schemas and TypeScript
      types (derived via z.infer) for Site, SiteConfig, ThemeTokens (locked superset:
      9-role palette, 9-step type scale with weights and lineHeights, 10-step geometric
      spacing, 4 container slots), NavConfig, NavPattern, NavEntry (discriminated
      union by kind: page/anchor/url), Page, ModuleInstance, Dials, ModuleContent,
      ContentValue (string | number | boolean | null | AssetRef | ContentValue[] |
      { [key: string]: ContentValue } — widened in REQ-6 to admit plain objects),
      AssetRef, SeoMeta. validateSite(input: unknown) returns Result<Site, ValidationError[]>
      with JSON-pointer-style error paths. Validation enforces required shape, primitive
      types, hex color regex, theme token slot completeness, and structural uniqueness
      (module IDs unique within page, page slugs unique within site). Catalog membership
      validation (whether ''hero'' is a real module type / variant / dial value) is
      deliberately NOT performed — that boundary belongs to the framework at render
      time.'
    justification: No existing story covers the site-definition data contract. This
      is the foundational schema imported by every other framework package, the static
      generator, the public-site Worker, and the builder SPA. The schema evolved across
      REQ-3 (initial slot list), REQ-4 (widened theme tokens to the locked superset),
      and REQ-6 (widened ContentValue for object-shaped fields) — all aspects of the
      same single contract, so they belong in one story.
    story_uid: story-aecb7377
  - index: 3
    component: Framework Tokens & CSS Generator
    item_type: feature
    story_points: 2
    dependencies:
    - 2
    description: '@1stcontact/framework token system: re-exports ThemeTokens from
      site-schema, ships default values for every slot (neutral palette, system fonts,
      standard scale), and exposes generateThemeCss(tokens, options?: { dark? }) which
      emits a CSS file declaring deterministically-named custom properties on :root
      (--color-<role>, --font-family-<name>, --font-size-<step>, --font-weight-<name>,
      --line-height-<name>, --space-<step>, --radius-<name>, --shadow-<name>, --container-<name>,
      --breakpoint-<name>) with an @media (prefers-color-scheme: dark) block when
      a dark palette is provided. Partial input fills missing slots from defaults.
      Also includes a vetted Google Fonts shortlist (Inter, Manrope, Fraunces, Playfair
      Display, Space Grotesk, DM Serif Display, Outfit, Sora, Source Sans 3, IBM Plex
      Sans, Lora, Merriweather, Work Sans) exposed via VETTED_FONTS, findFontByFamilyDeclaration(),
      and googleFontsHref() helpers.'
    justification: No existing story covers theme tokens or the CSS generator. The
      CSS generator is the user-visible bridge between abstract token data and browser
      styling — distinct enough from the module catalog to merit its own story (modules
      consume the generated CSS variables but don't depend on the generator's internals).
      The vetted fonts shortlist (added in REQ-6) belongs here because it's the same
      token domain (typography family declarations).
    story_uid: story-e53ba4cf
  - index: 4
    component: 'Framework Module Catalog: Chrome'
    item_type: feature
    story_points: 3
    dependencies:
    - 2
    - 3
    description: '@1stcontact/framework module system: TypeScript module contract
      (id, version, variants, dials, contentSchema via moduleMeta) and a typed registry
      mapping id → { meta, Component } with getModule(id, version) helper that throws
      a CatalogMissError on unknown lookups and lists all registered modules. Three
      Astro chrome modules conform to the contract: header (top-nav variant — logo
      left, links right, responsive hamburger collapse below breakpoint.md), hero
      (bg-color and bg-image variants; dials size/align/spacingTop/spacingBottom/surface;
      optional eyebrow/CTA), and footer (minimal variant — logo, tagline, copyright
      with build-time year constant, optional small-link row). Every module ships
      scoped CSS referencing tokens only (no inline styles). Browser-safe @1stcontact/framework/meta
      subpath exports just the module metas (no Astro components, no Node-fs styles)
      so the builder SPA can bundle the catalog without pulling in server-only dependencies.'
    justification: No existing story covers the framework module registry or any chrome
      module. The chrome modules together prove the module contract end-to-end and
      form the structural skeleton of every customer site (top nav, hero section,
      footer). Grouped with the registry because the registry is the contract surface
      they all exercise; splitting registry from chrome would create test-only and
      infrastructure-only stories that don't pass the user-visible-capability bar.
    story_uid: story-1d5b450f
  - index: 5
    component: 'Framework Module Catalog: Content'
    item_type: feature
    story_points: 3
    dependencies:
    - 4
    description: 'Three Astro content modules conforming to the moduleMeta contract:
      text-block (prose variant using container.narrow / landing variant using container.default;
      markdown body rendered with headings, lists, links, images, blockquotes, code
      blocks; optional heading; omitted when not provided), services-grid (three-col
      and two-col variants both collapsing to single column below breakpoint.md; items
      array validated 2..6; each item is { icon?, title, body markdown, cta? }), and
      contact-form (inline variant; server-rendered HTML that submits via plain HTML
      POST without JS; progressive-enhancement client island intercepts submit, posts
      JSON to action URL, replaces form with successMessage on 200, surfaces inline
      error on non-200; hidden honeypot field; data-turnstile-target mount point).
      validateModuleContent supports the new content shapes (list-of, object, enum)
      introduced by these modules. Registry extends to include all six Phase 0 modules.'
    justification: No existing story covers content modules. Distinct from chrome
      modules because content modules introduce new behaviors (markdown rendering,
      list-count validation, progressive enhancement client JS, honeypot/Turnstile
      mount, content-shape validation) that chrome modules don't exercise. Grouped
      together because the three content modules together complete the Phase 0 catalog
      and share the same content-validation extensions.
    story_uid: null
  - index: 6
    component: Static Site Generator
    item_type: feature
    story_points: 3
    dependencies:
    - 2
    - 3
    - 4
    - 5
    description: 'tools/generate package: file-backed static site generator. Programmatic
      API runGenerate() and CLI fc-generate --site <path> --out <path> [--clean].
      Pipeline: reads <site>/site.json plus recursive <site>/assets/**, validates
      against @1stcontact/site-schema (returns SiteLoadError with JSON-pointer error
      report on schema failure), renders each ModuleInstance through the framework
      registry using Astro''s experimental_AstroContainer (each instance wrapped in
      <div id="<id>" data-module-instance="..."> so nav anchors resolve), generates
      per-site theme.css by concatenating generateThemeCss(theme) output with module
      <style> blocks extracted via loadModuleStyles(), and emits <head> containing
      viewport, SEO (title/description/og:image), Google Fonts preconnect+preload+stylesheet
      derived from the site''s typography families via the vetted shortlist, and <link
      rel="stylesheet" href="/assets/theme.css">. Writes pages, theme.css, and assets
      to the output dir; --clean wipes the output dir first. The CLI shim re-execs
      under vite-node so the Astro vite plugin can compile .astro imports.'
    justification: No existing story covers the static generator. This is a distinct
      user-visible capability (a CLI tool plus a programmatic API) that consumers
      can invoke directly to build any site from a definition; it is not implied by
      the framework modules or the public-site Worker. Validation, rendering, asset
      copying, font preloading, and SEO emission are all behaviors of this single
      generator and belong in one story.
    story_uid: null
  - index: 7
    component: Public Site Worker
    item_type: feature
    story_points: 2
    dependencies:
    - 6
    description: 'sites/1stcontact/site.json: real Phase 0 marketing site definition
      for 1stcontact.io — single ''home'' page with nav.pattern in-page-anchors and
      the seven-module layout (header → hero → text-block landing → services-grid
      three-col → text-block prose → contact-form → footer); Manrope/Inter typography,
      primary #2563eb, accent #f59e0b. apps/public-site Worker: assets binding ASSETS
      pointing at ./public both at top level and under env.production.assets so production
      also serves from generated output; GET/HEAD requests delegated to ASSETS; unknown
      paths return plain-text 404. apps/public-site/package.json gains a generate
      script invoking fc-generate against sites/1stcontact; build/deploy/dryrun all
      generate first. CI and deploy workflows order a ''Generate public-site static
      output'' step before tests/dry-run/wrangler-deploy so generated artifacts are
      present.'
    justification: No existing story covers the 1stcontact marketing site or the public-site
      Worker's static-asset serving behavior. Distinct from the generator (item 6)
      because the generator is a reusable tool; this story documents the specific
      1stcontact site definition plus the Worker that serves the generated output
      and the workflow ordering that keeps generation and deploy consistent. The /api/forms/contact
      endpoint is covered by item 8 (the lead-capture pipeline replaced its stub).
    story_uid: null
  - index: 8
    component: Lead Capture Pipeline
    item_type: feature
    story_points: 3
    dependencies:
    - 7
    description: 'db/migrations/0001_create_leads.sql creates the leads table (id
      PK, site_id, form_id, created_at unix ms, name, email NOT NULL, phone, message,
      extra_fields JSON, page_path, user_agent, ip_country, turnstile_pass 0/1, status
      NOT NULL DEFAULT ''new'' with CRM Lite lifecycle enum new/contacted/quote_needed/quote_sent/follow_up/booked/lost/archived,
      notes) plus indexes leads_site_created and leads_status. apps/public-site/wrangler.toml
      binds LEADS_DB (D1), vars TURNSTILE_SITE_KEY/RESEND_NOTIFY_TO/RESEND_NOTIFY_FROM/SITE_ID,
      and documents TURNSTILE_SECRET/RESEND_API_KEY secrets and operator setup. apps/public-site/src/forms.ts
      POST /api/forms/contact handler: validates content-type application/json (400
      INVALID_CONTENT_TYPE on mismatch), parses body (400 INVALID_JSON on parse failure),
      silently drops honeypot submissions (200 success with no DB write), validates
      email present (400 MISSING_FIELD) and well-formed (400 INVALID_EMAIL), verifies
      Turnstile token via challenges.cloudflare.com/turnstile/v0/siteverify (400 TURNSTILE_FAILED
      on failure with no DB write and no Resend call), reads CF-IPCountry for ip_country,
      generates ULID, INSERTs into leads, posts to Resend best-effort (Resend failure
      logs but does NOT fail the request — the lead is already persisted), stores
      non-canonical submission fields in extra_fields JSON, returns 200 {success:true,
      message, lead_id}. tools/generate emits the Turnstile script tag and fc-turnstile-site-key
      meta on pages containing a contact-form when TURNSTILE_SITE_KEY is set. contact-form
      client island reads window.turnstile.getResponse() and attaches turnstile_token
      to the JSON body when a widget is present.'
    justification: No existing story covers D1 migrations, Turnstile verification,
      Resend notification, or the real lead-capture handler. This is a tightly coupled
      vertical (schema + handler + Turnstile widget loading + client token attachment
      + best-effort email) — splitting would force test-only stories. The stub /api/forms/contact
      endpoint from REQ-6 was deleted and replaced in REQ-7, so the lead-capture pipeline
      is the only form-handling behavior in current code.
    story_uid: null
  - index: 9
    component: Builder UI
    item_type: feature
    story_points: 3
    dependencies:
    - 2
    - 3
    - 4
    - 5
    description: 'packages/builder-ui: vanilla-DOM Phase 0 chat-driven site builder
      (BuilderStore + BuilderLayout + ChatPanel + PreviewPanel + chat-driver). Two-panel
      layout — collapsible chat left, iframe preview right, draggable splitter between;
      chat collapses to a 32px restore rail left of the preview, panel widths and
      collapsed state persisted to localStorage; preview fills the panel height. Viewport
      switcher with three presets — mobile 375px, tablet 768px, desktop 100% of pane.
      Eight v1 AI tool calls validated client-side against the framework catalog before
      applying to state: set_module_content, set_module_dial, set_module_variant,
      add_module, remove_module, reorder_modules, set_theme_token, set_site_config.
      Four-layer validation contract: catalog membership → schema validate → store
      ingest → render. Invalid tool calls rejected with a structured error surfaced
      in the chat log and store unchanged. apps/control-app: /builder and /builder/
      routes rewrite to /builder.html served from Workers Static Assets (other paths
      pass through); /api/chat POST proxies the Anthropic Messages API using the bound
      CLAUDE_API_KEY (default model claude-sonnet-4-6), with the full v1 tool surface,
      a system prompt containing the framework catalog, and extraction of text + tool_use
      blocks (500 on missing key, 502 on upstream failure). apps/control-app build/deploy
      scripts invoke build-builder-bundle.mjs which esbuild-bundles builder-ui/spa.ts
      to public/_assets/builder.js. packages/framework gains an in-browser render
      path (render/browser.ts) and the browser-safe /meta subpath so the catalog can
      be bundled without Astro server runtime.'
    justification: No existing story covers the builder SPA, its chat/preview UX,
      the AI tool surface, or the Anthropic proxy endpoint. This is a single end-to-end
      interactive capability — splitting along chat vs preview vs tool-validation
      vs Anthropic-proxy lines would create test-only or infrastructure-only stories.
      The vanilla-DOM choice (vs the ticket's named React) is an implementation detail;
      the matrix documents the behaviors users see.
    story_uid: null
---

# Reconciliation Plan

**Mode**: commits
**Anchor**: bundle-94e1d1b6 (BUNDLE-2 — REQ-1 + REQ-2 + REQ-3 + REQ-4 + REQ-5 + REQ-6 + REQ-7 + REQ-8)
**Subject**: bundle-94e1d1b6 (bundle is itself an indexable intent type per ticket_types.yaml)
**Commits analyzed**: 8 (ac174d8, f53c972, e34f13e, d4f8e4d, 1befb47, bce23fd, fa41944, 0b1ba59)
**Existing capability matrix**: empty (no prior capability or story tickets)

## Behavior Inventory

```yaml
behavior_inventory:
  source: "free-coded commits — 8 commits spanning REQ-1..REQ-8"
  entry_files:
    - "apps/public-site/src/index.ts"
    - "apps/public-site/src/forms.ts"
    - "apps/control-app/src/index.ts"
    - "apps/control-app/src/chat.ts"
    - "apps/control-app/public/builder.html"
    - "packages/site-schema/src/{schema,validate,index}.ts"
    - "packages/framework/src/tokens/{contract,defaults,css,fonts}.ts"
    - "packages/framework/src/modules/{registry,types,validate,shared,styles}.ts"
    - "packages/framework/src/modules/{header,hero,footer,text-block,services-grid,contact-form}/index.astro"
    - "packages/framework/src/render/browser.ts"
    - "packages/builder-ui/src/{store,main,spa,catalog,chat-driver,tools,preview}.ts"
    - "packages/builder-ui/src/components/{builder-layout,chat-panel,preview-panel}.ts"
    - "tools/generate/src/{index,cli,load,render,output}.ts"
    - "db/migrations/0001_create_leads.sql"
    - ".github/workflows/{ci,deploy}.yml"
    - "sites/1stcontact/site.json"
  features:
    - name: "Cloudflare Workers deploy pipeline"
      description: "pnpm monorepo, two Workers, GitHub Actions for CI and deploy-on-push-to-xgd-stable"
      behaviors:
        - "control-app returns text/plain placeholder Hello from app.1stcontact.io at /"
        - "deploy.yml triggers on push to xgd-stable, has concurrency group, deploys both Workers via wrangler --env production"
        - "ci.yml triggers on PR, runs pnpm test, runs dry-runs for both Workers"
        - "All slugs aligned to 1stcontact (root package name, both wrangler names, sites/ subdir, CLAUDE.md heading)"
      entry_point: "apps/control-app/src/index.ts; .github/workflows/*.yml"

    - name: "@1stcontact/site-schema validation"
      description: "Zod schemas + validateSite() returning typed Result with JSON-pointer error paths"
      behaviors:
        - "validateSite accepts minimal and full sites"
        - "Rejects invalid module shape, invalid nav pattern, missing theme slot, invalid hex color"
        - "Enforces structural uniqueness (module IDs within page, page slugs within site)"
        - "Locks theme token superset (9-role palette, 9-step type scale with weights+lineHeights, 10-step geometric spacing, 4 container slots)"
        - "Catalog membership deliberately NOT validated (framework's job at render time)"
        - "ContentValue admits string|number|boolean|null|AssetRef|array|object"
      entry_point: "packages/site-schema/src/validate.ts"

    - name: "generateThemeCss"
      description: "Emits :root custom properties for every theme slot with deterministic naming + dark-mode block + defaults"
      behaviors:
        - "Produces :root { --color-bg, --space-4, --font-family-heading, ... } covering every slot"
        - "Partial input fills missing slots from defaults"
        - "Optional dark palette emits @media (prefers-color-scheme: dark) block"
        - "Vetted Google Fonts shortlist + helpers (VETTED_FONTS, findFontByFamilyDeclaration, googleFontsHref)"
      entry_point: "packages/framework/src/tokens/css.ts"

    - name: "Framework module catalog (chrome)"
      description: "Registry, contract, and three chrome modules (header/hero/footer)"
      behaviors:
        - "getModule(id, version) returns the component or throws CatalogMissError"
        - "Every module exports moduleMeta conforming to the contract (id, version, variants, dials, contentSchema)"
        - "header top-nav: renders logo + entries, collapses to hamburger below md"
        - "hero bg-color: renders without <img> background element"
        - "hero bg-image: renders background image element with src/alt; omits CTA when not provided"
        - "footer minimal: renders copyright with build-time year + holder, optional links row"
        - "Browser-safe /meta subpath exports just module metas"
      entry_point: "packages/framework/src/modules/registry.ts and per-module Astro components"

    - name: "Framework module catalog (content)"
      description: "Three content modules (text-block/services-grid/contact-form) completing the 6-module Phase 0 catalog"
      behaviors:
        - "text-block prose uses container.narrow; landing uses container.default; markdown body with image/list; optional heading"
        - "services-grid three-col renders 3 cards; two-col renders 2; collapses to single column below md; rejects item count outside 2..6"
        - "contact-form renders configured fields, action attribute uses configured URL, includes honeypot hidden field, renders Turnstile mount point"
        - "contact-form submits without JS via HTML POST"
        - "contact-form client island intercepts submit, posts JSON, renders success message on 200, renders error on non-200"
        - "Registry includes all six Phase 0 modules"
        - "validateModuleContent handles list-of, object, enum content shapes"
      entry_point: "packages/framework/src/modules/{text-block,services-grid,contact-form}/index.astro and validate.ts"

    - name: "tools/generate static site generator"
      description: "File-backed generator validating + rendering site definitions to HTML/CSS/assets"
      behaviors:
        - "Validates site.json against schema; reports JSON-pointer errors as SiteLoadError"
        - "Emits index.html with module anchors and doctype"
        - "Emits per-site theme.css with theme tokens + module styles"
        - "Copies assets to output"
        - "Preloads configured fonts (preconnect + preload + stylesheet)"
        - "CLI fc-generate --site <path> --out <path> [--clean]"
      entry_point: "tools/generate/src/{cli,index}.ts"

    - name: "1stcontact marketing site + public-site Worker"
      description: "Real sites/1stcontact site definition served by apps/public-site via Workers Static Assets"
      behaviors:
        - "Worker serves generated index.html on GET /"
        - "Worker serves generated theme.css"
        - "Worker returns 404 for unknown paths"
        - "deploy.yml and ci.yml run the generate step before wrangler deploy/dryrun"
        - "sites/1stcontact/site.json defines the Phase 0 marketing page (header, hero, text-block landing, services-grid, text-block prose founder note, contact-form, footer) with Manrope/Inter and the primary/accent palette"
      entry_point: "apps/public-site/src/index.ts + sites/1stcontact/site.json"

    - name: "Lead capture pipeline"
      description: "D1 leads schema + POST /api/forms/contact with Turnstile + Resend"
      behaviors:
        - "Migration 0001 creates leads table with expected columns + indexes"
        - "POST inserts lead with all canonical fields"
        - "POST returns lead_id in response"
        - "POST writes ip_country from CF-IPCountry header"
        - "POST persists lead even when Resend call fails (best-effort notification)"
        - "POST drops honeypot submissions silently (returns 200 success with no DB row, no Resend call)"
        - "POST rejects invalid content-type, malformed JSON, missing email, invalid email format, failed Turnstile (each with distinct error code)"
        - "POST persists non-canonical fields into extra_fields JSON"
        - "Generator emits Turnstile script tag + fc-turnstile-site-key meta when contact-form present and TURNSTILE_SITE_KEY set"
        - "contact-form client island attaches turnstile_token to JSON body when widget present"
      entry_point: "apps/public-site/src/forms.ts + db/migrations/0001_create_leads.sql"

    - name: "Builder SPA + chat orchestration"
      description: "Vanilla-DOM two-panel chat-driven editor with iframe preview, AI tool calls, Anthropic proxy"
      behaviors:
        - "/builder and /builder/ rewrite to /builder.html via Workers Static Assets"
        - "Panel collapse/restore (32px rail left of preview); state persists across reload"
        - "Splitter drag resizes panels, clamps to min, persists final width"
        - "Viewport switcher resizes iframe to 375/768/100% for mobile/tablet/desktop"
        - "Preview fills panel height"
        - "Tool call applies to preview (stubbed set_theme_token updates store and preview CSS in lockstep)"
        - "Invalid tool call rejected by four-layer validator; state unchanged; structured error in chat log"
        - "Site definition + chat-turn mutations persisted to localStorage; survive store re-instantiation"
        - "POST /api/chat invokes Anthropic Messages API with bound CLAUDE_API_KEY, tool definitions, system prompt; returns text + tool_use; 500 on missing key, 502 on upstream failure"
      entry_point: "apps/control-app/src/chat.ts + packages/builder-ui/src/{spa,main,store,chat-driver}.ts"
```

## Coverage Map

```yaml
coverage_map:
  # No prior capabilities or stories exist — all features are uncovered.
  - feature: "Cloudflare Workers deploy pipeline"
    status: uncovered
    existing_stories: []
    existing_acs: []
    gaps: ["entire surface — pipeline, CI, deploy, control-app placeholder, slug alignment"]

  - feature: "@1stcontact/site-schema validation"
    status: uncovered
    existing_stories: []
    existing_acs: []
    gaps: ["entire surface — schema shape, validateSite, error paths, token-superset shape, ContentValue widening"]

  - feature: "generateThemeCss"
    status: uncovered
    existing_stories: []
    existing_acs: []
    gaps: ["entire surface — token CSS generator, defaults, dark mode, fonts shortlist"]

  - feature: "Framework module catalog (chrome)"
    status: uncovered
    existing_stories: []
    existing_acs: []
    gaps: ["entire surface — registry, contract, header/hero/footer, /meta subpath"]

  - feature: "Framework module catalog (content)"
    status: uncovered
    existing_stories: []
    existing_acs: []
    gaps: ["entire surface — text-block, services-grid, contact-form, validateModuleContent"]

  - feature: "tools/generate static site generator"
    status: uncovered
    existing_stories: []
    existing_acs: []
    gaps: ["entire surface — CLI, programmatic API, validation, render pipeline, font preloading"]

  - feature: "1stcontact marketing site + public-site Worker"
    status: uncovered
    existing_stories: []
    existing_acs: []
    gaps: ["entire surface — site definition, Worker serving via Static Assets, 404, workflow ordering"]

  - feature: "Lead capture pipeline"
    status: uncovered
    existing_stories: []
    existing_acs: []
    gaps: ["entire surface — D1 schema, handler validation, Turnstile, Resend, generator Turnstile injection, client token attachment"]

  - feature: "Builder SPA + chat orchestration"
    status: uncovered
    existing_stories: []
    existing_acs: []
    gaps: ["entire surface — SPA shell route, panel/splitter/viewport UX, tool validation, store persistence, Anthropic proxy"]
```

## Plan Items

| # | Component | Type | Points | Deps | Description |
|---|-----------|------|--------|------|-------------|
| 1 | Deploy Infrastructure | feature | 2 | — | Monorepo + 2 Workers + CI/deploy workflows + slug alignment |
| 2 | Site Schema | feature | 2 | 1 | site-schema Zod + validateSite + token superset + ContentValue widening |
| 3 | Framework Tokens & CSS Generator | feature | 2 | 2 | generateThemeCss + defaults + dark mode + fonts shortlist |
| 4 | Framework Module Catalog: Chrome | feature | 3 | 2, 3 | registry + contract + header/hero/footer + /meta subpath |
| 5 | Framework Module Catalog: Content | feature | 3 | 4 | text-block/services-grid/contact-form + validateModuleContent |
| 6 | Static Site Generator | feature | 3 | 2, 3, 4, 5 | tools/generate CLI + render pipeline + asset copy + font preload |
| 7 | Public Site Worker | feature | 2 | 6 | 1stcontact site definition + Worker static-asset serving + 404 + workflow ordering |
| 8 | Lead Capture Pipeline | feature | 3 | 7 | D1 leads + handler + Turnstile + Resend + generator injection + client token |
| 9 | Builder UI | feature | 3 | 2, 3, 4, 5 | Builder SPA + tool validation + Anthropic /api/chat proxy |

**Total: 9 items, 23 story points, all `feature` (no upgrades — capability matrix was empty).**

## Observations

- **Empty capability matrix**: `xgd ticket list --type capability` and `--type story` both returned no tickets, so reuse-first bias defaults all items to `feature`. There were no existing stories to extend.
- **Bundle subject_uid**: `bundle-94e1d1b6` is itself a first-class intent type (per `ticket_types.yaml` per the planner prompt), so subject_uid points at the bundle directly rather than an upstream epic. All eight bundled REQ source tickets are covered via the pre-fetched commits evidence.
- **REQ-1 placeholder for public-site is intentionally NOT in this plan**: the `test_UAT_FC_REQ-1_public_site_returns_placeholder` test was deleted in REQ-6 (`bce23fd`) because the placeholder behavior was superseded by serving the generated marketing site. Documenting the deleted placeholder would contradict current code. The control-app placeholder (`Hello from app.1stcontact.io`) remains and IS covered by item 1.
- **REQ-6 form stub is intentionally NOT in this plan**: the three `test_UAT_FC_REQ-6_form_stub_*` tests were deleted in REQ-7 (`fa41944`) because the real lead-capture handler replaced the stub. Form behavior is documented only via item 8.
- **REQ-2 (rename) folded into item 1**: the 1stcontact slug rename is configuration alignment with the deploy infrastructure, not a separate user-visible capability. Item 1 captures the alignment.
- **REQ-4's token system + module registry split into items 3 and 4**: the CSS generator operates on raw token data while modules consume the generated CSS variables — functionally separable user-visible capabilities. Combining would create a single 5-point story, exceeding the 3-point ceiling.
- **Chrome modules (item 4) and content modules (item 5) split deliberately**: content modules introduce new behaviors (markdown rendering, list-count validation, progressive-enhancement client JS, honeypot/Turnstile mount, content-shape validation) that chrome modules don't exercise. Combining would yield ~25 UATs in one story.
- **Generator (item 6) vs public-site (item 7) split**: the generator is a reusable CLI/programmatic tool; the public-site story documents the specific 1stcontact site definition plus the Worker that serves the output. Different consumers and different surfaces.
- **Builder UI (item 9) NOT split**: the chat panel, preview panel, tool validation, store persistence, and Anthropic proxy are tightly coupled — splitting along these lines would create test-only or infrastructure-only stories that don't pass the user-visible-capability bar.
- **Vanilla-DOM vs React (REQ-8)**: the operator note explicitly captures that the implementation diverged from the ticket's named React components to vanilla DOM. The matrix documents what users see (panels, splitter, preview, tool validation), not the framework choice — no plan item is needed for this divergence.
- **Two builder UATs added post-commit**: `test_UAT_FC_REQ-8_preview_fills_panel_height` and `test_UAT_FC_REQ-8_restore_rail_left_of_preview` exist on disk but were not in the original REQ-8 commit listing. Both refine the layout behaviors already named in item 9's description (preview fills panel height, chat collapses to 32px rail left of preview). Story-cycle UAT generation will pick these up as ACs in item 9.
- **Schema evolution across REQ-3/4/6 collapsed into item 2**: the ThemeTokens superset (REQ-4) and ContentValue widening (REQ-6) are evolutions of the same single contract introduced in REQ-3. The story documents the final schema shape; mid-bundle widenings are an implementation history, not separate capabilities.