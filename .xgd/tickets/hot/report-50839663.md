---
uid: report-50839663
id: REPORT-917
type: report
title: 'Code Review: bundle-93cd5926'
created_by: xgd
created_at: '2026-06-30T07:02:30.517087+00:00'
updated_at: '2026-06-30T07:02:30.517087+00:00'
completed_at: null
last_field_updated: created_at
result: fail
fields:
  report_kind: code_review
  subject_uid: bundle-93cd5926
  anchor_uid: bundle-93cd5926
---

# Code Review

**Result**: FAIL

## Summary

The bundle (REQ-53 + REQ-51 + BUG-15 + BUG-17) is otherwise high-quality, well-documented, fully tested work: `preview_generated_page` is correctly wired end-to-end, the render-by-default refactor cleanly deletes the old escalation path (no legacy mode), asset-inlining (BUG-15) and the budget bump (BUG-17) are sound. **One blocking issue**: the `requestOrigin` field added to `ActionContext` is orphaned plumbing — it is populated by two producers but read by zero handlers after the data:-URL amendment (commit d278044f) removed its only consumer, and its doc comment still falsely claims it is in use. This violates the project's explicit no-orphaned-code / ruthless-refactoring standard and must be removed before merge.

## Quality Gates

- **Lint**: success — 0 errors, 0 warnings (quality report report-fe2bbaa6).
- **Build**: success.
- **Tests**: The reconcile quality report is vacuous (0 tests — known `quality.yaml` test_dirs misconfiguration, pre-existing). Full `npx vitest run` executed manually: **1087 passed, 1 skipped, 2 failed**. Both failures are `test_UAT_FC_REQ-6_public_site_*` — an `ENOTEMPTY` rmdir race on the shared `apps/public-site/public/assets` dir plus a 30s build timeout. These tests are untouched by the bundle (`git diff main..HEAD` shows no `apps/public-site` or REQ-6 changes), exercise zero bundle code, and fail due to pre-existing public-site build/isolation flakiness — NOT a bundle regression. Every bundle-specific test passes (REQ-51 x14, BUG-15, BUG-17, REQ-22 render-by-default, reconciliation_*).

## External Interface Accessibility

New entry points wired in: **mostly yes, one orphan**.
- `preview_generated_page` registered in `SYSTEM_ACTIONS` and handler bound (registry.ts:489+) — OK.
- `<PreviewDigestReport>` re-exported (builder-ui/src/index.ts) and registered at boot via `registerPreviewDigestReport()` (builder-ui/src/main.ts) — OK.
- New extractor types/helpers (`PreviewDigest`, `PreviewSource`, `renderPreviewDigest`, `buildPreviewPrefix`, `parsePreviewDigest`, `parseReferenceDigest`) re-exported from extractor/src/index.ts — OK.
- `[browser]` top-level binding added to wrangler.toml — OK.
- **GAP**: `ActionContext.requestOrigin` (registry.ts:50) is dead. Producers: router.ts:67 (`safeOrigin`) and chat.ts:456 (`requestOriginOf`). Readers: none (`grep -rn '\.requestOrigin' apps packages` over src finds zero handler reads; the only mention in preview-generated-page.ts is a comment describing the abandoned approach). Both producer helpers (`safeOrigin`, `requestOriginOf`) exist solely to feed this dead field.

## Code Quality

| File | Finding | Severity |
|------|---------|----------|
| apps/control-app/src/operator/registry.ts:44-50 | `requestOrigin` field on `ActionContext` is never read by any handler — orphaned after the data:-URL amendment removed its consumer. Doc comment falsely states it is "Used by `preview_generated_page` to construct an absolute URL." | Critical |
| apps/control-app/src/operator/router.ts:67,109 | `safeOrigin()` helper + its call exist only to populate the dead `requestOrigin` field. | Critical (same issue) |
| apps/control-app/src/chat.ts:456,1005-1009 | `requestOriginOf()` helper + its call exist only to populate the dead `requestOrigin` field. | Critical (same issue) |
| tests/_helpers_REQ-21/22/28/51 + 4x REQ-46/REQ-33 tests | `requestOrigin` threaded through every `ActionContext` test literal to satisfy the now-pointless required field. | Warning (follows from removal) |
| apps/control-app/src/operator/preview-generated-page.ts | Otherwise excellent: clean degraded-mode handling, BUG-15 asset inliner is exhaustive (src + url()) with graceful per-asset failure, content-addressed draftId, multimodal two-image delta prompt. No issues. | — |
| packages/extractor/src/upload-screenshots.ts | `UploadScreenshotsArgs` discriminated union cleanly reuses the helper for the new prefix family instead of cloning. Good. | — |
| packages/web-fetch-safety/src/browser-budget.ts | BUG-17 constant bump is correct; enforcement machinery + config override intact. Good. | — |
| analyze-page.ts / extractor index / escalate.ts | Escalation path fully deleted (escalate.ts removed, `shouldEscalateToRendered`/`Escalation*` types removed, `forceRendered` input removed, stale escalation tests deleted) — exemplary full replacement, no legacy mode. | — |

## Checklist Compliance

No architecture, security, or design checklist reports exist for this anchor (`xgd ticket list --type report --filter fields.report_kind=*_checklist` returns 0 items). Sections skipped.

## Smoke Test

`preview_generated_page` is a Cloudflare Worker operator action, not a standalone CLI/HTTP entry point invokable outside a wrangler env. Smoke coverage is provided by `test_UAT_FC_REQ-51_tool_registered.test.ts` (3 tests asserting the tool is registered, discoverable, and dispatchable through the registry) plus a clean `build: success`. The handler compiles and is reachable through the action registry. Adequate.

## Issues Found

**Critical (must fix)**:
- Remove the orphaned `requestOrigin` apparatus. It is dead code routed through a core shared interface, and its doc comment actively misleads. This contradicts CLAUDE.md "No Legacy Modes" and the Coding Standards "Orphaned Files: Delete old files in the same commit that introduces their replacements."

**Warnings (should fix)**:
- The REQ-6 public-site tests are flaky (filesystem rmdir race + build timeout) independent of this bundle. Out of scope for this fix loop, but worth a follow-up ticket so the full-suite gate isn't permanently red.

## Fix-It Prompt

Remove the orphaned `requestOrigin` plumbing entirely (it has no readers after the data:-URL approach landed). Specifically:

1. `apps/control-app/src/operator/registry.ts` — delete the `requestOrigin: string | null` field and its doc comment from the `ActionContext` interface (lines ~44-50).
2. `apps/control-app/src/operator/router.ts` — delete `requestOrigin: safeOrigin(request)` (line ~67) and the now-unused `safeOrigin()` helper (line ~109). Verify `safeOrigin` has no other callers before deleting.
3. `apps/control-app/src/chat.ts` — delete `requestOrigin: requestOriginOf(request)` (line ~456) and the now-unused `requestOriginOf()` helper (lines ~1005-1009). Verify no other callers.
4. Remove `requestOrigin` from every `ActionContext` literal in tests: `tests/_helpers_REQ-21_analyze_page.ts`, `tests/_helpers_REQ-22_rendered.ts`, `tests/_helpers_REQ-28_transcribe_site.ts`, `tests/_helpers_REQ-51_preview.ts` (both the optional field on its opts type and the literal), and `tests/test_UAT_FC_REQ-46_xgd_ticket_*` (3 files) + `tests/test_UAT_FC_REQ-33_write_text_asset.ts`.
5. The comment in `preview-generated-page.ts:72-78` referencing `{requestOrigin}/assets/...` is fine — it correctly explains why the data: URL approach is used INSTEAD; leave it, but ensure it does not imply `requestOrigin` is still read.
6. Re-run lint + build + the affected suites to confirm green.

Do NOT instead try to "use" `requestOrigin` in the handler — the data:-URL navigation (BUG-15) is the correct, deliberate design; `requestOrigin` is genuinely vestigial and should be deleted, not revived.
