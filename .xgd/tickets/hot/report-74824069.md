---
uid: report-74824069
id: REPORT-646
type: report
title: 'Resync resolve conflicts: 393ea07289005dc2b83294d2dbb95d9e3563c1d3'
created_by: xgd
created_at: '2026-06-27T02:39:26.967485+00:00'
updated_at: '2026-06-27T02:39:26.967485+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: report-ebefc874
---

## Files resolved

- `apps/control-app/src/operator/router.ts` — **DU** (deleted by us / modified by them). Incoming = REQ-51 (9947c690), which adds `requestOrigin: safeOrigin(request)` to the `ActionContext` built in `handleOperatorActionRequest`, plus the `safeOrigin()` helper. The "deletion" on our side is not a legitimate refactor — `router.ts` exists and is wired (via `index.ts`) on `xgd-working`, the resync source line. Incoming is meaningful developer code, and the enrichment rule (take the more-recent side, theirs) agrees. Resolved with `git checkout --theirs` + `git add` → staged as A.
- `tests/_helpers_REQ-21_analyze_page.ts` — **DU**. Incoming = REQ-51, adds `requestOrigin: "https://app.test"` to the test harness's `ActionContext`. This helper is live on `xgd-working`: imported by 4 REQ-21 analyze_page UATs (`..._returns_valid_digest`, `..._kv_cache_returns_cached_digest`, `..._safety_failures_propagate_as_errors`, `..._sparse_signals_serialize_as_not_detected`). The deletion was resync replay churn, not a deliberate removal. Resolved with `git checkout --theirs` + `git add` → staged as A.

## Incoming changes preserved

- `router.ts`: confirmed `requestOrigin: safeOrigin(request)` (line 67) and `function safeOrigin(req: Request)` (line 109) present in the staged (`:`) version.
- `_helpers_REQ-21_analyze_page.ts`: confirmed `requestOrigin: "https://app.test"` (line 126) present in the staged version.

Both DU markers cleared; `git status --porcelain` shows no conflict-class lines. The full REQ-51 changeset (preview-generated-page handler, PreviewDigest schema, PreviewDigestReport card, REQ-51 UATs, etc.) is staged as M/A.

## ⚠️ Post-merge review flag (out of scope for this resolution, pre-existing)

Independent of the two files above, the resync branch HEAD is missing two operator modules that committed code depends on:

- `apps/control-app/src/operator/events.ts` — imported by `chat.ts` (`sessionEventBus`, `SseEvent`) and `registry.ts`.
- `apps/control-app/src/operator/types.ts` — imported by `chat.ts` (`extractSession`, `Session`) and `registry.ts`. The restored `router.ts` also imports both.

Both files exist on `xgd-working` but were **never added in any resync-line commit** (`main..HEAD` empty for them) — evidence that earlier DU resolutions in this resync run dropped them while keeping the code that imports them. The branch will not type-check/build until `events.ts` and `types.ts` are restored (and `index.ts` wires `router.ts` as on `xgd-working`). This is a systemic earlier-mis-resolution issue, not introduced by this step. Flagging for post-merge review / a corrected replay of the commits that introduce those modules.
