---
uid: report-4569334f
id: REPORT-890
type: report
title: 'Reconciliation Review: commits (BUNDLE-9)'
created_by: xgd
created_at: '2026-06-30T05:16:57.371294+00:00'
updated_at: '2026-06-30T05:16:57.371294+00:00'
completed_at: null
last_field_updated: created_at
result: fail
fields:
  report_kind: reconciliation_review
  subject_uid: bundle-44f53d53
  anchor_uid: bundle-44f53d53
---

# Reconciliation Review: Story Coverage

**Result**: FAIL
**Mode**: commits
**Surface**: (commits / bundle)
**Anchor**: bundle-44f53d53 (BUNDLE-9 — REQ-23 + REQ-24 + REQ-25 + REQ-50 + BUG-8)
**Stories Reviewed**: 4 (STORY-69, STORY-70, STORY-46, STORY-38; items 3+4 both target STORY-46)

## Summary

Intent fidelity and story-level coverage are sound across all five plan items —
including the highest-risk fidelity check (REQ-25's operator-driven scope
reduction that dropped the multi-session UI). The review FAILS on **Step 5b
evidence sufficiency**: the two acceptance criteria the bundle MODIFIED on
STORY-46 (AC-486, AC-585) have committed UATs that still encode the
*superseded* client-history / browser-transcript contract and now FAIL, so the
rewritten criteria have no passing evidence. The same `POST /api/chat`
contract change broke a wide set of pre-existing committed UATs that were
neither regenerated nor deleted. The full vitest suite — the real quality gate
(`include: tests/**/*.test.ts`) — is RED: **18 failed | 1035 passed (16 files)**.

These failures were masked during the run because the `reconciliation_test`
gate executed only UATs changed in this cycle (REPORT-881 / REPORT-884 /
REPORT-887 / REPORT-878 / REPORT-874 all report "No test files to execute"),
so the newly-generated AC-811..821 UATs pass while the stale committed UATs
were never run. This is the stale-evidence / [FREE-CODED]-overwrite hazard the
project guidance warns about.

## Behavior Inventory

~30 behaviors across 5 components. Per-component verdicts:
- **STORY-69 (item 1, REQ-23 schema)** — PASS. Migrations 0006/0007/0008 + reverse-order downs, chat_sessions/chat_messages/reference_docs, FTS5 + sync triggers, indices, cascades, TS records. All 9 operator ACs covered by behavioral UATs over real Miniflare D1 (7 reconciliation + 21 FC, all pass on direct run).
- **STORY-70 (item 2, REQ-24 HTTP API)** — PASS. 8 endpoints (session CRUD/pagination, atomic append+denorm via D1.batch, R2 attachment sweep, reference-doc list/read+section). 11 ACs, all covered by real-handler UATs over Miniflare D1; the two declared divergences (create returns full record @201; section-not-found falls back to full body) are explicitly flagged in the story, not silently absorbed.
- **STORY-46 (items 3+4, REQ-24 chat.ts + REQ-25 UI + BUG-8)** — FAIL (see below). Implementation code is correct; the *evidence* for the two modified ACs is stale and red.
- **STORY-38 (item 5, REQ-50 @gendev rename)** — PASS. Scope rename, four empty skeletons, ui-kit removal. AC-821 is genuinely behavioral and honestly asserts the control-app build STILL fails (pre-existing DOM-type error) — no green-build overclaim.

## Coverage Map (material rows)

| # | Behavior | Coverage | Story / AC | Notes |
|---|----------|----------|------------|-------|
| 1 | Chat/reference-doc schema, FTS, cascades, indices | Covered | STORY-69 AC-793..799 | Behavioral UATs, pass |
| 2 | Chat session + reference-doc REST API | Covered | STORY-70 AC-800..810 | Behavioral UATs, pass; divergences flagged |
| 3 | No-history-400, tail-prime≥5000, memory tools site-scope, first-turn title | Covered | STORY-46 AC-811..814 | New UATs rigorous, pass |
| 4 | Durable server-backed store, infinite-scroll+anchor, auto-single-session, in-panel boot error, BUG-8 bound fetch | Covered | STORY-46 AC-815..818 | New UATs rigorous, pass |
| 5 | @gendev scope, skeletons, ui-kit removal, build parity | Covered | STORY-38 AC-819..821 | AC-821 behavioral; build failure honestly documented |
| 6 | POST /api/chat SSE turn (server-resident) — **modified AC** | **Uncovered (evidence)** | STORY-46 **AC-486** (acceptance_criterion-88cbfc9e) | Criterion correctly rewritten to server-resident; covering UAT is STALE and FAILS (expects 200, gets 400) |
| 7 | Chat history persistence (server-resident) — **modified AC** | **Uncovered (evidence)** | STORY-46 **AC-585** (acceptance_criterion-287af37d) | Criterion correctly rewritten; covering UAT is STALE and FAILS (throws "runChatTurn: no chat session id") |

## Intent Fidelity

- **REQ-25 scope reduction — FAITHFUL.** No active AC asserts the dropped affordances (session list, new-chat, inline title edit, delete, multiple-sessions-per-site). AC-816 correctly encodes the net-landed single-auto-session-per-(site,browser) model and explicitly states the panel exposes no such UI in v1. The backing API/store machinery legitimately remains, unexposed. No silent divergence.
- **AC-486 / AC-585 criteria — FAITHFUL (text).** Both bodies were rewritten to the server-resident model (AC-585 body: "not stored as a transcript in the operator's browser storage … restored from the server by loading the active session's tail"). Only AC-585's title string is cosmetically stale ("persisted to browser storage").
- **REQ-50 control-app build failure — FAITHFUL.** AC-821 documents that the pre-existing control-app DOM-type build failure is neither introduced nor fixed by the rename; no AC claims a fully green build.
- **Divergence absorbed at the evidence layer (the FAIL):** the matrix criteria for AC-486/AC-585 were updated to the new contract, but their UATs (and a swath of sibling chat UATs) were left at the old contract. The matrix says one thing; the evidence proves the opposite (and fails).

## Step 5b — Evidence Sufficiency (FAILURES)

Verified by direct execution of the full suite (`npx vitest run`): **18 failed | 1035 passed**.

CORE (modified ACs with no passing evidence):
- **AC-486** (acceptance_criterion-88cbfc9e, active/pending, regression_only=false) — `tests/test_UAT_AC486_chat_endpoint_proxies_anthropic.test.ts` posts an old-contract body and asserts 200; the server-resident handler returns 400/404. 2 cases FAIL.
- **AC-585** (acceptance_criterion-287af37d, active/pending, regression_only=false) — `tests/test_UAT_AC585_chat_history_persists_and_restores_across_remount.test.ts` exercises the superseded browser-transcript model and calls `runChatTurn` with no session id → throws. FAILS.

COLLATERAL stale UATs on STORY-46 broken by the same POST /api/chat contract change (history→400 or runChatTurn w/o session):
- AC-487, AC-553, AC-554, AC-580, AC-730, AC-734, AC-735.

CROSS-STORY chat UATs broken by the same contract change (not owned by this bundle but red in the gate):
- AC-551 (story-a07c8ed3), AC-605/AC-606 (analyze_page_action), AC-717 (story-e893e643), AC-631, AC-633, AC-634.

REQ-50 leak:
- `tests/test_UAT_FC_REQ-50_scope_rename_complete.test.ts` FAILS — its grep includes `tests/` and self-matches the literal `@1stcontact/` string inside `tests/test_UAT_AC819_workspace_packages_use_gendev_scope.test.ts`. Test hygiene (exclude pattern too narrow), not a rename defect; AC-819's own test passes.

## Plan Item Accounting

| Plan Item | Expected Story | Status |
|-----------|---------------|--------|
| 1. Chat & reference-doc persistence schema | story-1e174b7c (STORY-69) | ✓ produced |
| 2. Chat session & reference-doc HTTP API | story-721e8feb (STORY-70) | ✓ produced |
| 3. Chat handler: server-resident tail-prime + memory tools | story-ba9f2715 (STORY-46), AC-486 + AC-811..814 | ✓ produced |
| 4. Builder UI durable sessions + BUG-8 | story-ba9f2715 (STORY-46), AC-585 + AC-815..818 | ✓ produced |
| 5. @gendev scope + productization skeletons | story-067dc2f8 (STORY-38), AC-819..821 | ✓ produced |

All plan items produced output. No items dropped.

## Judgment Calls

- STORY-69/70/38 source-inspection-style assertions (index names via EXPLAIN QUERY PLAN; "skeleton exists"; scope-string presence) are acceptable: EXPLAIN checks observe the real query planner (behavioral); "empty skeleton exists" is inherently structural; and the scope rename is behaviorally backstopped by AC-821 importing `@gendev/generate` (resolves only if the rename propagated). Not failed.
- AC-585 stale title string — cosmetic only; body is correct. Not a fail on its own, but should be corrected alongside the UAT regeneration.
- The masking by the changed-only reconciliation_test gate is the systemic cause; the fix must run the FULL suite green, not just the changed-this-cycle set.

## Verdict

FAIL. Coverage gaps (insufficient evidence) on the two MODIFIED acceptance
criteria of STORY-46. The fix loop must, without changing the (correct)
implementation:

1. Regenerate or delete to the server-resident contract the stale UATs for the
   ACTIVE modified ACs: **AC-486** (acceptance_criterion-88cbfc9e) and
   **AC-585** (acceptance_criterion-287af37d). New UATs must seed a session in
   D1 and POST `{sessionId, userMessage, siteDefinition, frameworkCatalog}`
   (AC-486) and activate a session before `runChatTurn` + assert tail-load
   restore from the API (AC-585). Fix AC-585's stale title string.
2. Regenerate/delete the collateral stale STORY-46 UATs: AC-487, AC-553,
   AC-554, AC-580, AC-730, AC-734, AC-735.
3. Update the cross-story chat UATs broken by the contract change: AC-551,
   AC-605, AC-606, AC-717, AC-631, AC-633, AC-634.
4. Fix `tests/test_UAT_FC_REQ-50_scope_rename_complete.test.ts` exclude pattern
   so it does not self-match `@1stcontact/` inside the AC-819 test file.
5. Re-run the FULL vitest suite (`npx vitest run`) and confirm green — do not
   rely on changed-only test selection, which masked this regression.

The implementation code (apps/control-app/src/chat.ts, chat-db.ts,
packages/builder-ui/src/*) is correct and the genuinely-new ACs (AC-811..821)
are rigorously evidenced; this is a test-debt / evidence-staleness failure, not
a behavior defect.
