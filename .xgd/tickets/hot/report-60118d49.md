---
uid: report-60118d49
id: REPORT-653
type: report
title: 'Capability-Intent Alignment: Lead Capture & CRM Lite (level=ac)'
created_by: xgd
created_at: '2026-06-28T19:28:39.681031+00:00'
updated_at: '2026-06-28T19:28:39.681031+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-79556c4b
  level: ac
  violations: 0
  warnings: 0
  needs_review_count: 0
---

# Capability-Intent Alignment: Lead Capture & CRM Lite
# Level: ac

**Result**: PASS
**Violations**: 0
**Warnings**: 0
**Needs review**: 0

## Cumulative Intent Considered

The capability tree contains a single story (STORY-45 / story-37572647,
`story_kind=feature`), whose `intent_uid` is the bundle below. No other
intents touch the capability's story/AC tree.

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| BUNDLE-2 (bundle-94e1d1b6) | free_and_reconciled | merged_at_commit 8ebe122e (completed 2026-06-25) | Defines the end-to-end lead-capture pipeline: `leads` D1 schema, `POST /api/forms/contact` handler (content-type/JSON/honeypot/email/Turnstile validation, CF-IPCountry, extra_fields, status=new), best-effort Resend notification, generator Turnstile script injection, client island token attachment, error-code set INVALID_JSON \| INVALID_CONTENT_TYPE \| MISSING_FIELD \| INVALID_EMAIL \| TURNSTILE_FAILED \| INTERNAL | YES |

At `ac` level the story body is the working reference (story-level cycle
assumed run first). STORY-45's body is internally consistent and
self-coherent, so no escalation into the BUNDLE-2 body/comments was
required.

## Alignment Ledger

Element-by-element alignment of the 12 ACs to STORY-45's in-scope behavioral surface.

| Element | Aligned to (story-body in-scope bullet) | Outcome |
|---|---|---|
| AC-464 (leads table migration 0001 + schema + indexes) | "A `leads` table created by `0001_create_leads.sql` … plus the `leads_site_created` and `leads_status` indexes" | aligned |
| AC-465 (valid submission persists + 200 + lead_id) | "generates a unique lead id … inserts a row with status `'new'`, and returns a 200 JSON response containing the lead id" | aligned |
| AC-466 (honeypot → success, no row, no email) | "silently drops submissions whose honeypot field is filled (returns a success-shaped 200 with no DB row and no notification email)" | aligned |
| AC-467 (non-json content-type → 400 INVALID_CONTENT_TYPE) | "requires `application/json` content type" | aligned |
| AC-468 (malformed body → 400 INVALID_JSON) | "rejects malformed JSON bodies" | aligned |
| AC-469 (missing email → 400 MISSING_FIELD) | "validates that `email` is present" | aligned |
| AC-470 (malformed email → 400 INVALID_EMAIL) | "validates that `email` is … well-formed" | aligned |
| AC-471 (Turnstile configured + fails → 400 TURNSTILE_FAILED, no row, no email) | "when a Turnstile secret is configured, verifies the supplied `turnstile_token` and rejects the request when verification fails" | aligned |
| AC-472 (ip_country from CF-IPCountry header, null when absent) | "reads the `CF-IPCountry` from the request" | aligned |
| AC-473 (non-canonical fields → extra_fields JSON, null when none) | "any property … not part of the canonical contact field set is stored in the row's `extra_fields` JSON blob" | aligned |
| AC-474 (notification failure does not fail request) | "Resend failures are logged but never fail the request — the lead is the source of truth" | aligned |
| AC-475 (generator injects Turnstile script + site-key meta when key set) | "the generated HTML includes the Cloudflare Turnstile script tag and a `<meta name=\"fc-turnstile-site-key\">`" | aligned |
| AC-476 (client island attaches turnstile_token when widget rendered) | "reads the widget's response token and attaches it to the JSON body as `turnstile_token` before posting" | aligned |

STORY-45 is `story_kind=feature` → ACs are expected and present (12).

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | info | coverage | AC set (STORY-45) | — | The "Turnstile secret absent → skip verification and persist without token" branch (STORY-45 Technical Context, a local-dev convenience) has no dedicated AC. It is a degenerate case of the happy path already exercised by AC-465 (persist + 200 + lead_id). Not a behavioral gap. | none |
| 2 | info | coverage | AC-474 | — | Positive owner-notification dispatch ("a notification request is made on success") is not asserted by a standalone AC, but AC-474's verification necessarily exercises the dispatch (the mocked provider endpoint receives the request and returns 500 / throws). Notification surface is covered. | none |

## Notes for the Editor

No edits required. Consistency, coverage, and exclusivity all hold at the AC level:

- **Consistency**: every AC follows from a STORY-45 in-scope bullet; error-code names (INVALID_CONTENT_TYPE / INVALID_JSON / MISSING_FIELD / INVALID_EMAIL / TURNSTILE_FAILED) and the honeypot field (`website`) and canonical field set are consistent across AC-466/AC-473 and the story body.
- **Coverage**: the story's 13-item in-scope surface maps 1:1 onto AC-464..AC-476 (schema, the six validation/reject paths, persist+ip_country, extra_fields, best-effort notification, generator injection, client token attach).
- **Exclusivity**: no two ACs describe the same criterion — missing-email (AC-469) vs malformed-email (AC-470), content-type (AC-467) vs JSON-parse (AC-468), and persist (AC-465) vs ip_country (AC-472) are each genuinely distinct.

The two info findings document reasoning for future drift checks; neither is a fix.
