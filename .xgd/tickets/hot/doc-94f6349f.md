---
uid: doc-94f6349f
id: DOC-10
type: doc
title: Chat Session Persistence and AI Memory
created_by: xgd
created_at: '2026-06-16T23:22:57.297595+00:00'
updated_at: '2026-06-16T23:22:57.297595+00:00'
completed_at: null
last_field_updated: created_at
status: null
fields:
  doc_kind: architecture
---

# Chat Session Persistence and AI Memory

## 1. Purpose & Scope

The 1st Contact builder is a chat-driven product. Today the chat history lives in browser memory only (`BuilderStore.chatHistory` in `packages/builder-ui/src/store.ts`) and is lost on reload. This document commits the architecture for persistent, searchable, per-site chat sessions plus a platform-level **Reference Document** library the AI consults on demand.

The scope is deliberately narrow to the chat + memory substrate. It does NOT cover the Design Brief (per-site canonical decisions doc) — that is owned by [[DOC-9]]. The Brief is a separate artifact stored as a site asset; this doc is about **conversation history** and **AI-consulted reference material**.

Paired with:

- [[DOC-4]] — product vision
- [[DOC-5]] — platform architecture (Cloudflare substrate)
- [[DOC-7]] — framework principles
- [[DOC-8]] — builder UI principles
- [[DOC-9]] — Web Reference Fetching and the Design Brief (per-site Brief)

**This document commits to:**

- **Per-site chat sessions** — every session belongs to exactly one site. No cross-site references, no global "my chats" view.
- **Multiple sessions per site** — onboarding, theme tuning, content editing, etc. The operator can start a new chat at will; old chats remain accessible from a session list scoped to the current site.
- **Unbounded append-only growth** — no truncation, no summarization, no compaction in the persistence layer. Sessions just grow.
- **Tail-prime + tools** — the AI is primed with the last ~5k characters of the active session on every turn. To reach further it uses tools: `search_transcripts`, `read_session_range`, `list_reference_docs`, `read_reference_doc`.
- **Infinite-scroll UI** — the chat panel loads the last ~5–10k characters of the active session and lazily fetches older messages as the operator scrolls upward.
- **Cloudflare-native primary store** — D1 for sessions and messages (with FTS5 for full-text search), R2 for blob attachments (screenshots, fetched HTML, future digests). KV cache optional, not required.
- **Platform-level Reference Documents** — distilled product understanding (modules, philosophy, framework conventions, how to map references to our schema) lives in a dedicated `reference_docs` collection the AI consults via tools, not in the system prompt.
- **KMS-aware document shape from day one** — both transcripts and reference docs follow `summary + table of contents + sectioned body` so when the knowledge-management system lands it slots in with zero retrofit.
- **No XGD ticketing in the product runtime** — XGD's chat-ticket model is a development-time artifact for the team; the customer-facing product persists chat in its own D1 schema.

**This document deliberately does not commit to:**

- The KMS implementation itself.
- Semantic / vector search (Vectorize comes later; FTS5 is sufficient for v1).
- Auto-summarization or compaction (the AI's search tools cover navigation; we revisit only if priming becomes expensive).
- Multi-tenant identity model beyond a `user_id` slot we'll add now and gate later.
- A cross-chat "references library" UI for chat search results — the AI consumes search through tools; the user navigates through the session list.
- Reference doc editing UX in v1 — initial docs are seeded; operator editing is a follow-up.

---

## 2. Use Cases

### 2.1 Resume yesterday's conversation

Operator returns to their site. The builder loads with the most-recent chat selected and the last few thousand characters of conversation visible. They scroll up to see what they discussed last time, ask a follow-up. The AI is primed with that same tail.

### 2.2 Start a fresh thread on the same site

Operator clicks "New chat". A new session is created bound to the current site. The previous chat remains in the session list for that site, searchable, scrollable.

### 2.3 "What did we decide about the palette?"

Operator asks the AI a question whose answer lives in an older chat. The AI calls `search_transcripts(query="palette", site_id=…)`, gets matching message ranges, calls `read_session_range` to pull the context, answers grounded in the actual prior discussion.

### 2.4 The AI needs platform knowledge

Operator asks for something the AI doesn't have in its prompt context (e.g., "what dial values does the hero-split module support?"). The AI calls `list_reference_docs` → `read_reference_doc(slug="modules/hero-split")`. The system prompt stays small; deep knowledge is on-demand.

---

## 3. Core Model — Two Memory Surfaces

The AI has two distinct memory surfaces, with different cardinalities and lifetimes:

| Surface | Owner | Lifetime | Mutation pattern | Shape |
|---|---|---|---|---|
| **Chat sessions** | Per-site, per-thread | Unbounded; append-only | Append on every turn | Sequence of role-tagged messages |
| **Reference docs** | Platform-global | Long-lived; edited rarely | Distilled and curated | Markdown with summary + ToC + sections |

The **Design Brief** ([[DOC-9]]) is a third memory surface — per-site canonical decisions — and is orthogonal: it's a site asset, not a chat artifact, and is loaded by name when the AI needs the site's identity.

Key property: **chat sessions are durable but not authoritative**. They are the conversation record. Decisions extracted from chats migrate into the Design Brief (per-site) or, for product-shaping insights, into the reference doc set (platform-wide).

---

## 4. The Chat Session

### 4.1 Identity and scope

- A session has an opaque ID and a `site_id`. The session list and search are scoped to a single site; no cross-site access.
- Session title is AI-generated after the first turn (one-line summary) and operator-editable.
- Sessions have `created_at`, `updated_at`, `last_message_at`, and a denormalized `message_count` for fast list rendering.

### 4.2 Messages

- Append-only sequence ordered by integer `ord` (per-session monotonic; allocated server-side at append time to keep ordering canonical).
- Each message has a role (`user` | `assistant` | `system` | `tool_result`), a content string, optional `tool_calls_json`, and a timestamp.
- No editing or deletion of individual messages in v1 (mirrors the append-only model). Session-level delete is supported.

### 4.3 Full-text search

- A D1 FTS5 virtual table indexes message `content`.
- Search is always site-scoped: `WHERE site_id = ?` is a non-optional predicate.
- Returned matches include `session_id`, `ord`, and a content snippet — the AI uses these to call `read_session_range` for surrounding context.

### 4.4 Attachments

- Heavy blobs (screenshots, large fetched documents, future Reference Digests from [[DOC-9]]) live in R2.
- The message references attachments by key in `tool_calls_json` or as inline markdown image syntax pointing at `/assets/<key>`.
- Deletion of a session triggers a sweep of its referenced R2 keys.

---

## 5. Tail-Prime & AI Memory Tools

### 5.1 Priming protocol

On every chat turn for the active session:

1. The control-app loads the last ~5000 characters of the session (configurable; not 5000 messages — 5000 *characters* of concatenated content, walked back from the tail).
2. This tail is included verbatim in the messages array sent to the model — it's not a summary, it's the raw recent transcript.
3. The system prompt includes a short note: "You are looking at the tail of this session. Older context is available via the `search_transcripts` and `read_session_range` tools."
4. Reference docs are NOT inlined — only their index (slug + summary line) is. The AI fetches bodies on demand.

### 5.2 Tool surface

```
search_transcripts(query: string, limit?: number)
  → [{ session_id, session_title, ord, snippet, ts }]
  // scoped to the current site implicitly; no cross-site reach

read_session_range(session_id: string, from_ord: number, to_ord: number)
  → [{ ord, role, content, ts }]

list_reference_docs()
  → [{ slug, title, summary }]

read_reference_doc(slug: string, section?: string)
  → { title, summary, toc, body }  // body filtered to one section if `section` is provided
```

These four tools, combined with the existing builder action tools (`set_module_content`, etc.), give the AI bounded-prompt access to unbounded memory.

### 5.3 Why this approach

- **No summary risk.** Auto-summarization would hide intent under paraphrase. The AI reads source-of-truth message text every time.
- **No context-window arms race.** Tail size is a single tunable knob. If 5k feels short, dial it. Never need to compact.
- **KMS-ready.** When KMS lands, `list_reference_docs` returns the same shape (slug + summary), `read_reference_doc` may delegate section extraction to KMS, and `search_transcripts` may add a semantic-rank pass — none of this changes the tool signatures.

---

## 6. Reference Documents

Reference docs are the **distilled product understanding** for the 1st Contact AI: how the framework thinks, what the modules do, what the design vocabulary is, how to map external references into our schema, how to talk to operators about aesthetics. They are platform-global; every site's AI consults the same set.

### 6.1 Source material

Initial seed is distilled from foundational artifacts already in the project:

- [[DOC-4]] product vision → `reference_docs/product-vision.md`
- [[DOC-5]] platform architecture → `reference_docs/platform-architecture.md`
- [[DOC-7]] framework principles → `reference_docs/framework-principles.md`
- [[DOC-8]] builder UI principles → `reference_docs/builder-ui-principles.md`
- [[DOC-9]] Web fetching + Design Brief → `reference_docs/web-fetching.md`, `reference_docs/design-brief.md`
- Each module's schema → `reference_docs/modules/<id>.md`
- Selected CHAT-* design discussions → `reference_docs/design-decisions/<topic>.md`

Distillation means: condense rationale into AI-actionable knowledge, drop ticket-bureaucracy framing, normalize to the canonical shape.

### 6.2 Canonical shape

```markdown
# {title}

> Summary: 1–2 paragraph distillation, AI-read first.

## Table of contents
- {section-slug} — one-line description
- ...

## {section-slug}
...body...
```

The summary is what `list_reference_docs` returns. The ToC plus `read_reference_doc(slug, section)` is how the AI navigates without loading everything.

### 6.3 Storage

Reference doc bodies are stored in D1 (or R2 with D1 metadata — TBD in the schema REQ; bias toward D1-only for v1 simplicity). FTS5 indexes the body for cross-doc search if needed later.

### 6.4 Lifecycle

V1: docs are seeded via migration / build step; operator UI for editing is deferred. Operators (i.e., us, the team) edit via filesystem + CI. When the platform grows to support customer-authored reference material per site, that becomes a separate `site_reference_docs` table — not in scope here.

---

## 7. UI Behavior

### 7.1 Loading and scrolling

- On chat panel mount, load the last ~5–10k characters of the active session (single paginated API call).
- When the operator scrolls to the top of the visible buffer, fetch the next page upward (`GET /api/chats/:id/messages?before=:ord&limit=N`).
- No artificial loading spinner mid-scroll; preload as the operator approaches the top.

### 7.2 Session list

- Sidebar or dropdown in the chat panel listing all sessions for the current site, ordered by `last_message_at` descending.
- Each row shows title + relative timestamp.
- "New chat" action at the top.
- Selecting a session loads its tail.

### 7.3 No cross-site UI

- There is no global chat search UI, no "all my conversations" view. If the user switches sites, the session list updates.
- The AI's `search_transcripts` is likewise site-scoped — operators never see a result from another site, and neither does the AI on their behalf.

---

## 8. Storage & Schema (logical)

```
chat_sessions(
  id            TEXT PK,
  site_id       TEXT FK → sites.id,
  title         TEXT,
  created_at    INTEGER,
  updated_at    INTEGER,
  last_message_at INTEGER,
  message_count INTEGER DEFAULT 0
)

chat_messages(
  id            TEXT PK,
  session_id    TEXT FK → chat_sessions.id,
  ord           INTEGER,                  -- per-session monotonic
  role          TEXT,                     -- user | assistant | system | tool_result
  content       TEXT,
  tool_calls_json TEXT,
  ts            INTEGER,
  UNIQUE(session_id, ord)
)

chat_messages_fts  -- FTS5 virtual table over (content), bound to chat_messages

reference_docs(
  slug          TEXT PK,                  -- e.g. "modules/hero-split"
  title         TEXT,
  summary       TEXT,                     -- short distillation, returned by list tool
  toc_json      TEXT,                     -- [{section_slug, description}]
  body          TEXT,                     -- full markdown
  kind          TEXT,                     -- architecture | module | design-decision | ...
  created_at    INTEGER,
  updated_at    INTEGER
)

reference_docs_fts -- FTS5 virtual table over (title, summary, body)
```

Detailed migration content is specified in the schema REQ (see §11). This section names the shape; the REQ pins the SQL.

---

## 9. Lifecycle, GC, and Non-Goals

- **Session delete**: cascade-deletes messages, sweeps R2 attachment keys referenced from those messages.
- **Site delete**: cascade-deletes all sessions belonging to that site (and their attachments).
- **No archival distinction**: sessions don't have an "archived" status. They exist or they're deleted.
- **No per-message edit/delete** in v1.
- **No automatic pruning** by age or count.
- **No anonymous mode** — every session belongs to a site, which belongs to an account.

---

## 10. Forward Compatibility (KMS)

When the knowledge-management system arrives, it slots in without changing the tool surface:

- `list_reference_docs` and `read_reference_doc` already match the KMS-aware shape (summary + ToC + section read).
- `search_transcripts` gains an internal semantic-rank pass (Vectorize) but the response shape is unchanged.
- The summary and ToC fields are AI-maintained today and AI-maintained tomorrow; only the maintainer changes.

No migration is anticipated. Document shapes were chosen to make this true.

---

## 11. Decomposition into REQs

1. **REQ — D1 schema for chat sessions + reference docs**: migrations for `chat_sessions`, `chat_messages` + FTS5, `reference_docs` + FTS5. Depends on [[REQ-10]].
2. **REQ — Chat session API + AI memory tools**: control-app endpoints + Anthropic call refactor (tail-prime + 4 new tools).
3. **REQ — Builder UI chat persistence**: replace in-memory `BuilderStore.chatHistory`; session list + new-chat + infinite scroll.
4. **REQ — Reference docs library + initial seed**: seed content distilled from foundational DOCs / CHATs; build-time hydration of `reference_docs`.

---

## 12. Open Questions (deferred to REQs)

- Tail size — start at 5000 chars but make it a config knob from day one.
- Should attachments be a separate `chat_attachments` table or inline in `tool_calls_json`? (Lean toward separate to make GC simple — pinned in schema REQ.)
- `user_id` column on `chat_sessions` now or later? (Lean: add the column now nullable; populate when auth lands.)
- Reference doc storage — D1 column for body, or D1 metadata + R2 blob? (Lean: D1-only for v1; revisit if docs grow large.)
- Session title generation — sync-generated at end of first turn, or background job? (Lean: sync at end of first turn; if it fails, fall back to truncated first user message.)

---

## Appendix A — Transcript context

This document and its child REQs originate from a sequence of design conversations. The key excerpts are embedded in each REQ for resumability; the full conversations live in:

- [[CHAT-13]] — AI Web Access (introduces the unbounded-append + tail-prime + KMS-aware document model)
- This conversation — confirms per-site scoping and the reference-docs concept

When picking up work in an intent-specific session, the relevant excerpts are already attached to that intent's REQ.
