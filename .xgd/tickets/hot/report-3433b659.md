---
uid: report-3433b659
id: REPORT-572
type: report
title: 'Resync resolve conflicts: 393ea07289005dc2b83294d2dbb95d9e3563c1d3'
created_by: xgd
created_at: '2026-06-27T00:16:54.518038+00:00'
updated_at: '2026-06-27T00:16:54.518038+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: report-ebefc874
---

## Files resolved

- `packages/extractor/package.json` — DU (HEAD deleted, incoming modified). Incoming commit (56ea8189, `feat(extractor): transcription primitives [FREE-CODED]`) adds `@1stcontact/framework` and `@1stcontact/site-schema` workspace deps that the new `transcribe.ts` requires. Rule 2a applied: incoming is meaningful → `git checkout --theirs` then stage. Staged blob hash `81baa2ce` matches incoming theirs exactly.
- `pnpm-lock.yaml` — UU (both modified). Ours = reconcile bundle-94e1d1b6 (`fix_quality_batch_prompt completed: done`), an FSM bookkeeping commit; theirs = the same feat(extractor) commit, adding ~524 lines for the new workspace deps. Rule 2g/timestamp rule applied: incoming wins because the lock file must mirror the package.json changes landing in the same commit. Reconcile-side lock churn is regenerable. Staged blob hash `dd6ebb56` matches incoming theirs exactly.

## Incoming changes preserved

- `packages/extractor/package.json`: confirmed both `"@1stcontact/framework": "workspace:*"` and `"@1stcontact/site-schema": "workspace:*"` are present in the resolved file. Hash equality with incoming blob proves nothing was lost.
- `pnpm-lock.yaml`: resolved blob is byte-identical to incoming theirs blob (`dd6ebb56...`), so every lock entry the incoming commit introduced is preserved.

## Post-merge review flag

`packages/extractor/package.json` was deleted on the resync HEAD side without commit metadata available. Worth a post-resync glance to confirm no main-side intent (e.g. a deliberate package rename) was discarded by taking theirs. The rest of `packages/extractor/src/` is intact on HEAD, suggesting an isolated package.json deletion that the incoming commit correctly reinstates.
