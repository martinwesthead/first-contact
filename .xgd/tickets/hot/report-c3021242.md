---
uid: report-c3021242
id: REPORT-872
type: report
title: Claude reconciliation_uat_generation report
created_by: xgd
created_at: '2026-06-30T04:12:43.978458+00:00'
updated_at: '2026-06-30T04:12:43.978458+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: reconciliation_uat_generation
  subject_uid: story-1e174b7c
---

All 7 reconciliation UATs pass and conform to the enforced `test_UAT_AC\d+_\w+` naming convention — exactly one UAT per acceptance criterion, traced to AC-793 through AC-799.

## Completion Report

```
UATs generated for story story-1e174b7c (plan item 1 of 5)

Story: story-1e174b7c
Test file(s) created:
  - tests/test_reconciliation_chat_reference_schema.test.ts

Tests written: 7
Tests passed: 7
Tests failed: 0

test_files_created:
  - "tests/test_reconciliation_chat_reference_schema.test.ts"
```

### AC → UAT mapping (1:1)

| AC | UAT | Verifies |
|----|-----|----------|
| AC-793 | `test_UAT_AC793_chat_session_roundtrip_and_site_activity_list_index` | Session round-trips all fields (null user/title, default `message_count=0`, absent `last_message_at`); site-scoped newest-activity list uses `idx_chat_sessions_site_last` |
| AC-794 | `test_UAT_AC794_messages_strictly_increasing_ordinal_unique_and_tail_index` | Ordinal append + field round-trip (incl. `tool_calls_json`); duplicate `(session, ord)` rejected; tail query uses `idx_chat_messages_session_ord` |
| AC-795 | `test_UAT_AC795_message_fts_consistent_across_insert_update_delete` | Message FTS stays consistent across insert/update/delete on one connection |
| AC-796 | `test_UAT_AC796_message_fts_search_is_scoped_to_a_single_site` | Site-scoped FTS (join through session) isolates sites; empty for non-matching site |
| AC-797 | `test_UAT_AC797_site_and_session_delete_cascades_to_messages_and_fts` | Site delete → sessions/messages/FTS gone; session-only delete → its messages/FTS gone, site survives |
| AC-798 | `test_UAT_AC798_reference_docs_roundtrip_fts_and_kind_filter` | Doc field round-trip; FTS over title/summary/body independently; body update re-indexes; delete removes from search; kind filter uses `idx_reference_docs_kind` |
| AC-799 | `test_UAT_AC799_migrations_are_reversible_leaving_base_schema_intact` | Forward creates all tables/FTS/indexes/triggers; down migrations remove them all with no error; accounts/sites/revisions intact |

**Constraints honored:** No runtime code changed; no existing tests modified — one new test file only. Tests exercise the real SQLite/D1 boundary via Miniflare (matching the established `_helpers_REQ-23_db.ts` pattern), with index usage proven via `EXPLAIN QUERY PLAN` and FTS sync proven via `MATCH` counts. The pre-existing `test_UAT_FC_REQ-23_*` files remain on disk for the downstream FC-orphan phase to rename/delete.
