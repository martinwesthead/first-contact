---
uid: report-4d835336
id: REPORT-682
type: report
title: 'UAT Coverage: Static Site Generator'
created_by: xgd
created_at: '2026-06-28T20:31:47.987535+00:00'
updated_at: '2026-06-28T20:31:47.987535+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: uat_coverage_check
  subject_uid: capability-820fbc22
  violations: 0
  warnings: 0
  needs_review_count: 0
---

# UAT Coverage Assessment: Static Site Generator

**Result**: PASS
**AC verdicts**: 13 pass, 0 fail, 0 deprecated, 0 needs_review
**Story verdicts**: 1 pass, 0 fail, 0 stale, 0 needs_review
**Capability verdict**: pass

## Cumulative Intent Considered

Chronological ledger of intents that touched this capability:

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| BUNDLE-2 (bundle-94e1d1b6) | free_and_reconciled | 2026-06-15 (merged_at_commit 8ebe122e) | Founding reconciled bundle for Phase 0. Contains REQ-6 ("tools/generate + 1st Contact marketing site definition + wire public-site") which introduces the file-backed static site generator: `runGenerate` programmatic API, `fc-generate` CLI (--site/--out/--clean), site.json loading + schema validation (SiteLoadError + JSON-pointer report), HTML5 page emission at slug-derived paths, module anchor wrapping (id + data-module-instance), per-site theme.css (theme tokens + module styles), `<head>` SEO/OG metadata with fallbacks, vetted Google Fonts link emission, recursive asset copy to /assets/site/, and --clean wipe. | YES |

No later intent retires, narrows, or supersedes any generator behavior. The cumulative intent is exactly the founding REQ-6 surface; the matrix should describe all of it, and it does.

## Alignment Ledger

| Story | Intents aligned to | Outcome | Notes |
|---|---|---|---|
| STORY-43 (story-d111f966) | BUNDLE-2 / REQ-6 | aligned | Every behavior in the story body is supported by REQ-6. The Turnstile `<head>` emission referenced in Technical Context is explicitly delegated to the lead-capture story (story-lead-capture-pipeline) and intentionally not an AC of this story — not a gap. |

## Findings — Categorized by Editor Action

None. Zero violations, zero warnings, zero needs_review.

## Notes for the Editor

This is a model reconciliation suite. `tests/test_reconciliation_static_site_generator.test.ts` covers all 13 ACs (AC-443 … AC-455), each via real entry points and observable file-system output — no internal mocking:

- **Real programmatic API**: tests call `runGenerate({ site, out, clean })` from `@1stcontact/generate` and assert on the returned result object AND on the actual files written to a temp out-dir (pages, theme.css, copied assets), reading byte content back where relevant (AC-451 compares copied bytes; AC-447 asserts a recognizable `--color-primary` value AND a `.fc-*` module selector coexist in one file).
- **Real CLI**: AC-443 and AC-455 spawn `node tools/generate/bin/cli.mjs` as a real subprocess, asserting exit codes, stdout success summary, and stderr failure lines — including missing-flag usage errors and a forced schema-validation failure.
- **Negative / fallback paths exercised**: AC-449 covers the full seoMeta → page-title → site-tagline fallback chain across three pages; AC-450 asserts vetted fonts emit preconnect/preload/stylesheet (dedup'd family query) and that non-vetted families emit none; AC-452/AC-453 assert `SiteLoadError` with JSON-pointer paths for schema violations and missing/malformed `site.json`, and that no output dir is created on failure; AC-454 verifies both --clean (stale file removed) and no-clean (stale file preserved) branches.

Observations could plausibly distinguish a correct implementation from an incorrect one in every case; none are structural (source-text) or over-mocked. No editor action required.
