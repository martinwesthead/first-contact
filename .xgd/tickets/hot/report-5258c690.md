---
uid: report-5258c690
id: REPORT-645
type: report
title: 'Resync resolve conflicts: 393ea07289005dc2b83294d2dbb95d9e3563c1d3'
created_by: xgd
created_at: '2026-06-27T02:24:37.047928+00:00'
updated_at: '2026-06-27T02:24:37.047928+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: resolve_conflicts
  subject_uid: report-ebefc874
---

## Files resolved

Cherry-picked commit: 3847ff00 — `refactor(arch): rename npm scope @1stcontact → @gendev + seed productization packages (REQ-50) [FREE-CODED]`

**UU (both modified) — config files, rule 2g (scalar → incoming wins):**
- `apps/control-app/package.json` — conflict isolated to `name`/`version` scalars. Took incoming: `@gendev/control-app` / `0.0.2`. (The scope rename IS REQ-50's intent.)
- `packages/builder-ui/package.json` — same; took incoming: `@gendev/builder-ui` / `0.0.3`.

**DU (main deleted, incoming modified) — deletion is correct, `git rm`:**
For every file below, the incoming diff is *only* the mechanical import-scope rename (`@1stcontact/*` → `@gendev/*`); it carries no new developer logic. The file is absent on HEAD and unreferenced.

- `apps/control-app/src/safety/health.ts` — never in HEAD history, no references on HEAD. Incoming change = 2-line import rename only. Accepted deletion.
- `tests/test_UAT_FC_REQ-13_*.test.ts` (11 files) — FC tests reconciled away on main; zero `test_UAT_FC_REQ-13_*` remain on HEAD (renamed/merged into AC-named tests, e.g. `test_UAT_AC427_*`, `test_UAT_AC485_*`). Incoming change = import rename only. Accepted deletion.
- `tests/test_UAT_FC_REQ-21_digest_report_renders_from_tool_result.test.ts`, `tests/test_UAT_FC_REQ-21_end_to_end_chat_renders_digest.test.ts` — reconciled away on main. Import rename only. Accepted deletion.
- `tests/test_UAT_FC_REQ-9_tool_list_filters_by_plan_tier.test.ts` — reconciled away on main. Import rename only. Accepted deletion.

No test coverage was lost: these FC tests were already reconciled into AC-named equivalents on main (the FC-orphan invariant). The DU deletions accept main's legitimate post-reconcile state, not churn.

## Incoming changes preserved

- `apps/control-app/package.json`: incoming `@gendev/control-app` name + version `0.0.2` present in resolved file.
- `packages/builder-ui/package.json`: incoming `@gendev/builder-ui` name + version `0.0.3` present in resolved file.
- DU files: incoming side carried only the `@1stcontact → @gendev` scope rename. That rename is fully reflected everywhere else in the tree (control-app deps already `@gendev/*`, etc.). The deleted files' sole incoming change (import-string rename of a file that no longer exists on main) is moot — preserving it would resurrect FC orphans the reconcile already removed. No developer logic discarded.

## Verification

- `git status --porcelain` shows zero conflict-class entries (UU/AA/DU/UD/AU/UA).
- No conflict markers remain in resolved files; both package.json parse as valid JSON.
- Staged tree has a net change vs HEAD; zero unmerged paths.
