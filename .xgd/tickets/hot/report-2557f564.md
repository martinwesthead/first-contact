---
uid: report-2557f564
id: REPORT-667
type: report
title: Fix Public Site Delivery (uat) — attempt 1
created_by: xgd
created_at: '2026-06-28T20:02:10.636276+00:00'
updated_at: '2026-06-28T20:02:10.636276+00:00'
completed_at: null
last_field_updated: created_at
fields:
  report_kind: fix_structural_validation
  subject_uid: capability-474ee896
  level: uat
  fixes_applied: 4
  progress_made: true
  needs_more_work: false
  violations_remaining: 0
  anchor_report_uid: report-cda4212b
---

# Fix Summary — Public Site Delivery (uat)

**Attempt**: 1
**Fixes applied this call**: 4
**Violations remaining**: 0
**Needs more work**: false

Both `uat-add` coverage violations are resolved. Per the report's "Notes for
the Editor", this was purely additive test work — production code (`index.ts`,
`wrangler.toml`) already satisfies both ACs and was NOT touched.

## Actions Taken — by Resolution Category

| # | Category | Element | Action |
|---|---|---|---|
| 1 | uat-add | AC-616 | Added `it("test_UAT_AC616_head_root_returns_200_via_assets_binding")` to the existing booted-worker suite `tests/test_UAT_AC458_AC459_AC460_public_site_worker_serves_generated_bundle.test.ts`. Issues `worker.fetch("/", { method: "HEAD" })` and asserts status 200. |
| 2 | uat-add | AC-615 | Created `tests/test_UAT_AC615_public_site_wrangler_declares_production_assets_block.test.ts` (3 `it()`s) — section-aware TOML read asserting top-level `[assets]` and `[env.production.assets]` each declare `directory="./public"` + `binding="ASSETS"`, and that the two directories match. |
| 3 | field-update | AC-616 | Set `uat_coverage=pass`. |
| 4 | field-update | AC-615 | Set `uat_coverage=pass`. |

## Test Verification

- **AC-615 test**: ran locally via `npx vitest run` — **3/3 passed** (static-config read, no worker boot needed).
- **AC-616 test**: rides the existing `unstable_dev` suite whose sibling tests
  (AC-458/459/460) are confirmed aligned-and-passing in the alignment ledger.
  Could not boot the worker locally in this worktree because its `node_modules`
  is symlinked to the main checkout, which has no `@1stcontact/*` workspace
  links; `pnpm install` would have wiped the shared store (declined). The test
  is verified-correct by inspection: production `apps/public-site/src/index.ts:24`
  delegates both GET and HEAD to `env.ASSETS.fetch`, and the generated bundle
  contains `index.html`, so HEAD `/` returns 200. It runs in the regression/CI
  environment where the workspace resolves.

## Design Notes

- AC-615 test does NOT reuse AC-389's `tomlTopName` helper, which deliberately
  stops at the first `[` section header (so it cannot read assets blocks). A
  small section-aware reader (`tomlSection`/`tomlString`) extracts the exact
  named tables — no TOML parser dependency exists in the repo.

## Code Edits

None this call. Production code already satisfies both ACs (confirmed in the report).

## Warning Not Actioned (out of scope)

Finding #3 (warning, `uat-edit (remove duplicates)`): the `FC_REQ-6` trio
duplicates AC-458/459/460 scenarios. Explicitly "opportunistic" and "does not
affect pass/fail". Left in place — removing tests is outside the scope of
closing the two coverage violations.

## needs_review Items Forwarded

None.
