---
uid: report-2219c327
id: REPORT-656
type: report
title: 'Reconciliation Plan: BUNDLE-4 (REQ-22 + REQ-28 + BUG-1 + REQ-30 + REQ-31)'
created_by: xgd
created_at: '2026-06-28T19:36:31.561331+00:00'
updated_at: '2026-06-28T21:18:00.585895+00:00'
completed_at: null
last_field_updated: items
fields:
  report_kind: reconciliation_plan
  subject_uid: bundle-24c4d23c
  anchor_uid: bundle-24c4d23c
  items:
  - index: 1
    component: Reference Digest Extraction (packages/extractor — rendered fetch path)
    item_type: upgrade
    story_points: 3
    dependencies: []
    target_story_ids:
    - story-3f73931a
    description: 'REQ-22 replaces STORY-55''s always-false static escalation hook
      with a real Browser-Rendering escalation path in packages/extractor: shouldEscalateToRendered
      (thin_body <200 visible chars, js_dominant >80% script bytes, forceRendered
      wins unconditionally); renderedFetch via the @cloudflare/puppeteer BROWSER binding
      with driver injection for tests (navigate, networkidle0/10s, three viewport
      screenshots, computed-CSS extraction); COMPUTED_EXTRACTION_SCRIPT (computed
      font-family/size/weight for body/h1/h2/h3, body + largest-above-the-fold background-color,
      computed background-image URLs for body/header/section/hero candidates); mergeComputedSignals
      (computed wins over declared typography/palette, folds computed background-image
      URLs into the asset inventory with URL-keyed dedup incrementing references);
      uploadScreenshots (mobile/tablet/desktop PNGs to ASSETS_BUCKET under references/{chatId}/{turnId}/{viewport}.png
      with an 8 MB per-viewport cap).'
    justification: 'STORY-55 explicitly scopes its escalation hook as ''always reports
      do not escalate in this version (the REQ-22 Browser Rendering integration point)''
      and excludes the rendered fetch path, computed CSS, and screenshots as ''owned
      elsewhere (REQ-22)''. REQ-22 is exactly that integration point: it extends the
      same packages/extractor library and the same Reference Digest asset-inventory
      bucket — no new capability bucket. Behaviour is proven by FC tests test_UAT_FC_REQ-22_{rendered_fetch_hydrates_spa,
      escalation_thin_body, escalation_js_dominant, escalation_force_rendered, merge_background_inventory,
      upload_screenshots} which have no ACs yet.'
    acceptance_criteria_changes:
      add:
      - shouldEscalateToRendered returns {escalate:true, reason:'thin_body'} for a
        body under 200 visible chars and {escalate:false} for rich text.
      - shouldEscalateToRendered returns {escalate:true, reason:'js_dominant'} when
        <script> content exceeds 80% of body bytes.
      - forceRendered:true makes shouldEscalateToRendered escalate unconditionally
        regardless of heuristic.
      - renderedFetch against the js-spa fixture returns hydrated html (>1000 visible
        chars) plus computed styles for body and h1.
      - mergeComputedSignals folds a computed background-image URL (external-stylesheet-bg
        fixture) into signals.assetInventory as kind:'background' that the static
        parse did not produce; an already-present URL increments references rather
        than duplicating; computed typography/palette win over declared.
      - uploadScreenshots writes mobile/tablet/desktop PNGs to ASSETS_BUCKET at references/{chatId}/{turnId}/{viewport}.png,
        each under the 8 MB cap, returning the keys.
      modify:
      - Replace the 'static-only escalation hook that always reports do not escalate'
        acceptance language with the real escalation heuristic + rendered fetch behaviour.
      remove: []
    intent_delta_summary: 'STORY-55 grows from a Layer-A-only deterministic extractor
      with a stub escalation hook to also own the Browser-Rendering rendered-fetch
      path: real escalation heuristic, computed-CSS extraction, computed background-image
      inventory merge, and screenshot upload. Prior Layer-A intent (deterministic
      signals, not_detected absence, markdown render) is preserved unchanged.'
    story_uid: story-3f73931a
  - index: 2
    component: Analyze action & Digest Report card (analyze_page rendered wiring +
      multimodal commentary + screenshot strip)
    item_type: upgrade
    story_points: 3
    dependencies:
    - 1
    target_story_ids:
    - story-15bae45e
    description: 'REQ-22 wires the rendered path into STORY-56''s analyze_page action
      and DigestReport card: analyze_page runs escalation -> budget gate -> rendered
      driver -> mergeComputedSignals -> R2 screenshot upload -> digest, adds the optional
      forceRendered tool-input parameter, and populates digest.screenshotKeys; budget
      exhaustion ([[REQ-20]] budget_exhausted) degrades to fetchPath:''static'' with
      a whatsMissing note and still returns ok (not ok:false); the Haiku 4.5 commentary
      pass becomes multimodal (desktop screenshot as an image content block; prompt
      asks for visual observations); BROWSER binding scoped to production in wrangler.toml
      (dev degrades to static); <DigestReport> renders a mobile/tablet/desktop screenshot
      strip as the first body element when digest.screenshotKeys is populated, served
      via /assets/{key}.'
    justification: STORY-56 already owns analyze_page and the Digest Report card,
      and explicitly defers 'Browser-rendering escalation, multimodal commentary using
      a screenshot — REQ-22 (the escalation hook always stays on the static path here)'.
      REQ-22 lifts that exact deferral on the same action and same card — an in-place
      extension, no new capability bucket or parallel action. Behaviour is proven
      by FC tests test_UAT_FC_REQ-22_{budget_exhausted_fallback, multimodal_commentary,
      digest_report_screenshot_strip, end_to_end_spa_chat_card} which have no ACs
      yet.
    acceptance_criteria_changes:
      add:
      - analyze_page escalates per shouldEscalateToRendered (or forceRendered) and
        runs renderedFetch -> mergeComputedSignals -> uploadScreenshots; the returned
        digest carries fetchPath:'rendered', computed typography (not not_detected),
        and all three screenshotKeys.
      - With the Browser-Rendering budget exhausted, analyze_page returns a digest
        with fetchPath:'static', a whatsMissing entry citing the exhausted budget,
        and ok:true (the call does not return ok:false).
      - The Haiku 4.5 commentary pass for a digest with a desktop screenshot includes
        the screenshot as an image content block and emits at least one sentence referring
        to visual properties (alignment/density/treatment/layout).
      - <DigestReport> renders the screenshot strip as the first element of the card
        body when digest.screenshotKeys is present; images resolve via /assets/{key}.
      - 'End-to-end: operator pastes a JS-SPA URL -> analyze_page -> escalation fires
        -> digest chat-card renders with all three screenshots, computed typography,
        and computed background-image URLs in the inventory.'
      modify:
      - Update the analyze_page tool description and acceptance language to add the
        optional forceRendered parameter and the rendered/screenshot/multimodal-commentary
        behaviour previously deferred to REQ-22.
      remove: []
    intent_delta_summary: STORY-56's analyze_page + DigestReport card gain the rendered-fetch
      wiring, forceRendered parameter, R2 screenshot upload, multimodal Haiku commentary,
      budget-exhaustion fallback, and the screenshot strip. The static-path digest,
      caching, intent-gating, and typed-error propagation intent is preserved unchanged.
    story_uid: story-15bae45e
  - index: 3
    component: Convert flow — transcribe_site mechanical orchestration
    item_type: feature
    story_points: 3
    dependencies:
    - 4
    - 5
    - 7
    description: 'transcribe_site is a Layer B operator action that mechanically converts
      a Reference Digest into a draft-reconstruction artifact (REQ-28 as reshaped
      by REQ-30 — net code is purely mechanical, no internal LLM synthesis). First
      invocation returns {kind:''convert_confirmation''}; after confirm_convert lands
      consent it runs sequential, SSE-emitting stages (apply_screenshot_preview ->
      deterministic theme-token derivation -> inline asset mirror -> build & write
      TranscriptionDigest to R2) returning {kind:''transcribe_site_done'', digestKey,
      summary:{pageCount, assetCount, mirrored, mirrorFailures}}. confirm_convert
      records per-chat consent and, with ownsSite=true, registers a per-origin robots
      override (in-memory chat-metadata store, demo trade-off — does not survive reload).
      The chat AI is the synthesizer: docs/llm-context/reproducing-a-website.md is
      imported as raw text and appended to buildSystemPrompt so the AI drives reconstruction
      via state_edit + page-CRUD tools. No legacy/synthesis fallback path remains
      (synthesis functions deleted per CLAUDE.md No Legacy Modes). Covers the mocked
      killer-demo and multi-page end-to-end flows.'
    justification: 'No existing story covers the convert/transcribe flow: STORY-56
      explicitly defers ''Layer B transcription that Convert this site eventually
      triggers — REQ-28 (this story only emits the request event)''. This is a genuinely
      new operator-facing capability bucket (paste URL -> reconstruct site) with no
      prior story to extend. Proven by FC tests test_UAT_FC_REQ-28_{transcribe_site_requires_confirmation,
      transcribe_site_stages} and test_UAT_FC_REQ-30_{synthesis_code_removed, system_prompt_includes_howto,
      killer_demo_mocked, multi_page_demo} which have no ACs.'
    story_uid: story-b3866352
  - index: 4
    component: Convert flow — TranscriptionDigest derivation & retrieval (packages/extractor
      + read_transcription_digest)
    item_type: feature
    story_points: 3
    dependencies:
    - 5
    description: 'Deterministic pure-function building blocks plus the read-back action
      for the convert flow: deriveThemeTokens (digest palette/typography/layout ->
      theme-token patch; not_detected signals leave framework defaults); extractPageContent
      (headings/paragraphs/nav-links/form fields -> ExtractedBlock[]); inferSuggestedModuleTypes
      (heuristic module-type ordering: header/hero first, contactForm when forms present,
      footer last — no LLM); slugFromUrl, titleFromDigest; buildTranscriptionDigest
      (assembles a TranscriptionDigest{siteId, sourceUrl, themeTokens, perPagePlan[],
      assetInventory[] with r2Keys, mirrorSummary} from the home digest + same-origin
      nav-discovered cached page digests). read_transcription_digest system_action
      reads sites/{siteId}/transcription/digest.json from ASSETS_BUCKET and returns
      the parsed JSON, or {status:''failed'', error contains ''digest_not_found''}
      when absent. Multi-page perPagePlan is unbounded (cardinality not enforced).'
    justification: These deterministic primitives + the digest read-back action are
      new convert-flow capabilities with no existing story (the digest derivation,
      per-page plan, and R2 digest contract did not exist before REQ-28/REQ-30). Separated
      from item 3 because they are pure packages/extractor functions + a read action
      distinct from the apps/control-app orchestration handler. Proven by FC tests
      test_UAT_FC_REQ-28_derive_theme_tokens_from_digest and test_UAT_FC_REQ-30_{digest_written_to_r2,
      read_transcription_digest, per_page_plan, asset_inventory_r2_keys} which have
      no ACs.
    story_uid: story-f45a5e61
  - index: 5
    component: Convert flow — asset mirroring to R2
    item_type: feature
    story_points: 3
    dependencies: []
    description: mirrorAssetToR2 fetches a URL through [[REQ-20]]'s safety layer (SSRF,
      rate limit, robots, 5 MB cap), classifies Content-Type to (extension, canonical
      type) for image png/jpeg/webp/svg/gif/avif + video mp4/webm/quicktime (unknown
      -> .bin), and writes bytes to ASSETS_BUCKET at the content-addressed key sites/{siteId}/imports/{sha256(url):16}.{ext};
      returns a structured outcome (ok/reason/detail) with explicit failure-reason
      mapping (body_too_large kept by name; private_ip/disallowed_scheme/too_many_redirects
      collapse to ssrf_blocked; requires_robots_override and rate_limited keep their
      names) so the chat 'What couldn't mirror' surface can name each failure. mirrorAssetBatchToR2
      downloads many URLs with a fixed concurrency cap (default 4), dedupes identical
      URLs so safeFetch is called once per unique URL, and returns successes + failures
      + a urlToR2Key map. urlContentHash gives the deterministic 16-char dedup key.
      assets-heavy and duplicate-asset fixtures exercise mixed success/failure and
      one-R2-object dedup.
    justification: Asset mirroring to R2 is a new convert-flow capability with no
      existing story — REQ-22 explicitly defers asset download ('downloads happen
      in REQ-28 Stage 4'), and STORY-54 (Asset Storage CRUD) is the generic R2 surface,
      not the safe-fetch + content-addressed import path. Kept separate because the
      safe-fetch + dedup + content-hash mirror loop is a self-contained subsystem
      reused by both the REQ-28 and REQ-30 orchestrations. Proven by FC tests test_UAT_FC_REQ-28_{mirror_asset_to_r2,
      fixtures_assets_heavy_dedup} which have no ACs.
    story_uid: story-5d1952ba
  - index: 6
    component: Convert flow — page CRUD state-edit tools
    item_type: feature
    story_points: 2
    dependencies: []
    description: 'Three new state_edit tools the chat AI uses to reconstruct a multi-page
      site (REQ-30, implementing [[REQ-14]]''s page-management section): add_page({slug,
      title, after_slug?}) inserts a page with empty modules + default seoMeta, validating
      slug format ^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?$, uniqueness, and after_slug existence;
      remove_page({slug}) removes a page, fails cannot_remove_only_page when one page
      remains, and atomically strips nav entries pointing at it; reorder_pages({slugs})
      reorders, failing validation when the list omits or repeats any slug. All run
      through the [[REQ-3]] validator and follow the [[REQ-9]] registry conventions.'
    justification: Page CRUD is a new operator/AI capability — REQ-30 notes add_page/remove_page/reorder_pages
      were specced in REQ-14 but never implemented; there is no existing story for
      page management. Distinct user-visible value (multi-page site construction).
      Proven by FC test test_UAT_FC_REQ-30_page_crud_tools (and exercised by multi_page_demo)
      which has no ACs.
    story_uid: story-e893e643
  - index: 7
    component: Convert flow — chat-card variants (ConvertConfirmation + TranscribeProgress)
    item_type: feature
    story_points: 2
    dependencies: []
    description: 'Two [[REQ-13]]-dispatcher-mounted builder chat-card variants for
      the convert flow: <ConvertConfirmation> (kind ''convert_confirmation'') — warning-tone
      card with the destructive-overwrite prompt + ''I own this site'' checkbox; Confirm/Cancel
      dispatch fc:convert-confirmed / fc:convert-cancelled CustomEvents so the chat
      driver wires confirm_convert and re-invokes transcribe_site. <TranscribeProgress>
      (kind ''transcribe_site_done'') — info-tone card with a 4-row stage list (Screenshot/Theme/Modules/Assets
      mirrored N/M) plus a ''What couldn''t mirror'' sub-section naming each Stage-4
      failure reason. applyTranscribeEvent(handle, event) updates the active card
      in place per SSE event without re-render. Both register via registerToolResultRenderer(kind,
      fn).'
    justification: These are new Builder UI chat-card variants for a flow that did
      not exist before; no existing Builder UI story covers convert-flow confirmation
      or progress surfaces. Kept distinct from item 3 because they are packages/builder-ui
      front-end components with their own dispatcher wiring. Proven by FC tests test_UAT_FC_REQ-28_{convert_confirmation_card,
      transcribe_progress_card} which have no ACs.
    story_uid: story-2524a1ae
  - index: 8
    component: Builder UI — chat input visibility (TipTap CSS, BUG-1)
    item_type: upgrade
    story_points: 1
    dependencies: []
    target_story_ids:
    - story-ba9f2715
    description: 'BUG-1 fixes the builder chat input that became invisible after the
      REQ-13 TipTap migration: apps/control-app/public/builder.html still styled the
      dead .fc-chat__textarea selector and had no rules for the new .fc-chat__editor
      / .fc-chat__editor-content elements, so the editor wrapper collapsed to zero
      size and Send took over the row. CSS-only fix: drop the orphan rule, add .fc-chat__editor
      (flex 1 1 auto, min-width 0, dark bg, border, focus-within highlight) and .fc-chat__editor-content
      (min/max-height, padding, colour, outline:none), and pin .fc-chat__input as
      a row with Send at flex-end.'
    justification: A bug fix to an existing capability — STORY-46 already owns the
      chat-driven builder SPA including the chat input. Bug fixes are upgrades (existing
      story not behaving as intended); no new capability bucket. Proven by FC test
      test_UAT_FC_BUG-1_chat_input_visible_and_positioned which has no AC.
    acceptance_criteria_changes:
      add:
      - The builder chat input editor element renders with non-zero width/height and
        the Send button sits to the right of the editor in the .fc-chat__input flex
        row (the shipped CSS defines .fc-chat__editor and .fc-chat__editor-content
        and no longer references .fc-chat__textarea).
      modify: []
      remove: []
    intent_delta_summary: STORY-46's chat-input acceptance is tightened to assert
      the TipTap editor is visible and correctly positioned (the CSS keeps pace with
      the REQ-13 DOM migration). No behavioural intent change beyond the bug correction.
    story_uid: story-ba9f2715
  - index: 9
    component: Builder UI — preview panel Reset button (REQ-31)
    item_type: upgrade
    story_points: 1
    dependencies: []
    target_story_ids:
    - story-ba9f2715
    description: 'REQ-31 adds a Reset button to the builder preview-panel toolbar:
      on click it shows a window.confirm prompt, and on confirm clears the 1stcontact_builder_site_v1
      localStorage key and reloads, returning the draft to the bundled baseline; cancel
      does nothing. bootBuilder wires the click; injection points (resetPrompt, reloadPage,
      storageKey) let UATs exercise the flow without an actual reload. Dev affordance
      for iterating on the convert flow.'
    justification: 'Extends STORY-46''s existing builder preview panel with one new
      toolbar affordance — reuse-first: the preview panel (with viewport presets,
      persistence to localStorage) already exists in STORY-46, so this is an in-place
      upgrade, not a new capability bucket or parallel UI. Proven by FC test test_UAT_FC_REQ-31_preview_panel_reset_button
      which has no AC.'
    acceptance_criteria_changes:
      add:
      - The preview panel toolbar renders a Reset button; clicking it shows a confirm
        prompt and, on confirm, removes the 1stcontact_builder_site_v1 localStorage
        key and triggers a reload; on cancel neither removeItem nor reload is called.
      modify: []
      remove: []
    intent_delta_summary: STORY-46's preview panel gains a Reset-to-baseline toolbar
      affordance. Existing preview intent (viewport presets, splitter, localStorage
      persistence) is preserved unchanged.
    story_uid: story-ba9f2715
---

# Reconciliation Plan — BUNDLE-4

**Mode**: commits
**Anchor**: bundle-24c4d23c (REQ-22 + REQ-28 + BUG-1 + REQ-30 + REQ-31)
**Subject (intent)**: bundle-24c4d23c (bundle is a first-class intent type for reconcile)

## Intent (Step 0)

Five bundled intents, read from the bundle body + amendments:
- **REQ-22** — Browser Rendering integration in packages/extractor: JS-rendered fetch path, computed-CSS signals (typography/palette/background-image), three-viewport screenshots to R2, multimodal Haiku commentary, DigestReport screenshot strip. (The 2026-06-24 render-by-default amendment in the body belongs to REQ-49 and is NOT in this bundle's commits — the free-coded commit 6da5585 implements the *escalation-heuristic* version, which is therefore the ground truth reconciled here.)
- **REQ-28** — Layer B convert flow: transcribe_site + confirm_convert, asset mirroring to R2, ConvertConfirmation/TranscribeProgress cards, theme-token derivation.
- **REQ-30** — convert-flow rework: transcribe_site becomes *purely mechanical* (internal Opus synthesis removed), writes a TranscriptionDigest to R2, adds read_transcription_digest, page-CRUD tools, and an AI-facing how-to doc wired into the system prompt. The chat AI is now the synthesizer.
- **BUG-1** — restore builder chat input visibility (CSS not updated for the REQ-13 TipTap migration).
- **REQ-31** — preview-panel Reset button (dev affordance).

**Net-state reconciliation note:** because REQ-30 reshapes REQ-28 within the same bundle, the matrix reflects the *cumulative* end state. Confirmed by code: transcribe.ts no longer exports composePromptForTranscription/buildSiteFromTranscription/validateTranscription/parseTranscriptionFromLlm/buildHeroOnlyFallback (grep clean); shouldEscalateToRendered exists with thin_body/js_dominant/forceRendered; page-CRUD tools, read_transcription_digest, and docs/llm-context/reproducing-a-website.md are present. REQ-28's deleted synthesis path is therefore NOT documented as a capability; only the surviving mechanical flow is.

## Behavior Inventory

```yaml
behavior_inventory:
  source: "commits-mode: 11 [FREE-CODED] commits on reconcile-BUNDLE-4"
  features:
    - name: "renderedFetch / escalation / computed signals (packages/extractor)"
      entry_point: "packages/extractor/src/{escalate,rendered-fetch,merge,upload-screenshots}.ts"
      behaviors:
        - "shouldEscalateToRendered: thin_body / js_dominant / forceRendered"
        - "renderedFetch via injected @cloudflare/puppeteer driver; 3 viewport screenshots; computed-CSS script"
        - "mergeComputedSignals: computed wins; background-image URLs merged into inventory with dedup"
        - "uploadScreenshots: R2 references/{chatId}/{turnId}/{viewport}.png, 8MB cap"
    - name: "analyze_page rendered wiring + DigestReport strip (apps/control-app, builder-ui)"
      entry_point: "apps/control-app/src/operator/analyze-page.ts; builder-ui digest-report.ts"
      behaviors:
        - "escalation -> budget gate -> driver -> merge -> R2 upload -> digest.screenshotKeys"
        - "forceRendered tool param; BROWSER binding scoped to production"
        - "budget_exhausted -> fetchPath:'static' + whatsMissing, ok:true"
        - "multimodal Haiku commentary with desktop screenshot image block"
        - "<DigestReport> screenshot strip first in card body"
    - name: "transcribe_site mechanical orchestration (apps/control-app)"
      entry_point: "apps/control-app/src/operator/transcribe-site.ts; chat-metadata.ts; chat.ts buildSystemPrompt"
      behaviors:
        - "requires_confirmation gate; confirm_convert consent + ownsSite robots override"
        - "stages: screenshot -> deriveThemeTokens -> inline mirror -> build+write TranscriptionDigest"
        - "SSE action:notify per stage; returns {transcribe_site_done, digestKey, summary}"
        - "system prompt appends reproducing-a-website.md; no synthesis fallback (No Legacy Modes)"
    - name: "TranscriptionDigest derivation + read-back (packages/extractor, operator)"
      entry_point: "packages/extractor/src/transcribe.ts; operator/read-transcription-digest.ts"
      behaviors:
        - "deriveThemeTokens, extractPageContent, inferSuggestedModuleTypes, slugFromUrl, titleFromDigest"
        - "buildTranscriptionDigest: perPagePlan via same-origin nav-link cache lookups (unbounded N)"
        - "read_transcription_digest: reads sites/{siteId}/transcription/digest.json or digest_not_found"
    - name: "asset mirroring to R2 (packages/extractor)"
      entry_point: "packages/extractor/src/mirror-asset.ts"
      behaviors:
        - "mirrorAssetToR2: safeFetch -> classify content-type -> sites/{siteId}/imports/{sha256:16}.{ext}"
        - "failure-reason mapping (body_too_large/ssrf_blocked/requires_robots_override/rate_limited)"
        - "mirrorAssetBatchToR2: concurrency 4, per-URL dedup, urlToR2Key map"
    - name: "page CRUD state-edit tools (builder-ui/tools.ts)"
      behaviors:
        - "add_page (slug format/uniqueness/after_slug), remove_page (only-page guard, nav strip), reorder_pages (exact-set)"
    - name: "convert-flow chat cards (builder-ui)"
      behaviors:
        - "<ConvertConfirmation> warning card + ownsSite checkbox + fc:convert-confirmed/cancelled"
        - "<TranscribeProgress> 4-row progress + 'What couldn't mirror'; applyTranscribeEvent in-place update"
    - name: "builder chat input CSS (BUG-1)"
      behaviors: ["editor visible, Send right-aligned; .fc-chat__editor rules replace dead .fc-chat__textarea"]
    - name: "preview-panel Reset button (REQ-31)"
      behaviors: ["confirm -> clear localStorage key + reload; cancel -> noop; injectable for UATs"]
```

## Coverage Map

```yaml
coverage_map:
  - feature: "renderedFetch/escalation/computed signals"
    status: partial
    existing_stories: [story-3f73931a]  # STORY-55, CAP-46
    gaps: ["escalation hook is a stub stub; rendered fetch/computed CSS/screenshots deferred to REQ-22 in story body"]
  - feature: "analyze_page rendered wiring + DigestReport strip + multimodal commentary"
    status: partial
    existing_stories: [story-15bae45e]  # STORY-56, CAP-46
    gaps: ["REQ-22 escalation/multimodal/screenshot explicitly deferred in story body"]
  - feature: "transcribe_site convert orchestration"
    status: uncovered
    existing_stories: []
    notes: ["STORY-56 defers Layer B transcription to REQ-28"]
  - feature: "TranscriptionDigest derivation + read_transcription_digest"
    status: uncovered
    existing_stories: []
  - feature: "asset mirroring to R2"
    status: uncovered
    existing_stories: []
    notes: ["STORY-54 is generic R2 CRUD, not the safe-fetch content-addressed import path"]
  - feature: "page CRUD tools"
    status: uncovered
    existing_stories: []
    notes: ["REQ-14 specced but never implemented before REQ-30"]
  - feature: "convert-flow chat cards"
    status: uncovered
    existing_stories: []
  - feature: "builder chat input CSS (BUG-1)"
    status: partial
    existing_stories: [story-ba9f2715]  # STORY-46, CAP-38
  - feature: "preview-panel Reset button (REQ-31)"
    status: partial
    existing_stories: [story-ba9f2715]  # STORY-46, CAP-38
```

## Plan Items

| # | Component | Type | Pts | Deps | Target |
|---|-----------|------|-----|------|--------|
| 1 | extractor rendered path (REQ-22) | upgrade | 3 | - | story-3f73931a |
| 2 | analyze_page wiring + DigestReport strip (REQ-22) | upgrade | 3 | 1 | story-15bae45e |
| 3 | transcribe_site mechanical orchestration (REQ-28/30) | feature | 3 | 4,5,7 | - |
| 4 | TranscriptionDigest derivation + read-back (REQ-28/30) | feature | 3 | 5 | - |
| 5 | asset mirroring to R2 (REQ-28/30) | feature | 3 | - | - |
| 6 | page CRUD tools (REQ-30) | feature | 2 | - | - |
| 7 | convert-flow chat cards (REQ-28) | feature | 2 | - | - |
| 8 | builder chat input CSS (BUG-1) | upgrade | 1 | - | story-ba9f2715 |
| 9 | preview-panel Reset button (REQ-31) | upgrade | 1 | - | story-ba9f2715 |

**Totals**: 9 items (feature: 5, upgrade: 4), 21 points.

## Observations & Judgment Calls

- **Render-by-default amendment is out of scope here.** The bundle body carries a 2026-06-24 amendment inverting escalation to render-by-default and removing shouldEscalateToRendered/forceRendered — but that is REQ-49 work and is NOT among this bundle's 11 commits. Ground truth (commit 6da5585 + current escalate.ts) is the escalation-heuristic version, so items 1–2 document that. A future REQ-49 reconcile will supersede these ACs.
- **REQ-30 supersedes REQ-28 within the bundle (Step 3b Case 2).** REQ-30 explicitly removes REQ-28's internal LLM synthesis and reshapes Stage 3. The matrix documents only the net mechanical flow; the deleted synthesis primitives are not given ACs. The 6 REQ-28 synthesis FC tests were deleted by commit 44c637a and 2 were relaxed — consistent with the net state.
- **Convert flow decomposed into 5 features, not 1.** The combined REQ-28+REQ-30 intent is ~20 story points spanning packages/extractor pure functions, an apps/control-app orchestration handler, a safe-fetch mirror subsystem, builder-ui chat cards, and new state-edit tools. Each is a separable, independently-testable capability sized at <=3 pts; collapsing them would exceed the 3-pt ceiling and mix front-end/back-end/pure-function concerns.
- **BUG-1 and REQ-31 both upgrade STORY-46 (CAP-38)** but are kept as distinct items: one is a chat-panel CSS regression fix, the other a new preview-panel affordance — different surfaces, different ACs.
- **Asset mirroring is a feature, not an upgrade of STORY-54.** STORY-54 (Asset Storage) is generic key-addressed R2 CRUD; the convert-flow mirror is a safe-fetch + content-addressed (sha256) import pipeline with failure surfacing — a new capability, not an extension of the CRUD surface.
- **Page CRUD closes REQ-14's page-management section** (per the REQ-30 reconcile note); REQ-14's nav-editing + duplicate_module sections remain open and are out of scope for this bundle.
- **All surviving FC tests are covered.** Every test_UAT_FC_{REQ-22,REQ-28,REQ-30,REQ-31,BUG-1}_* file on disk maps to exactly one plan item, so the story cycle can rename each to its new AC and check_fc_orphans will pass.