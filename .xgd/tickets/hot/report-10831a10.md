---
uid: report-10831a10
id: REPORT-675
type: report
title: 'Capability-Intent Alignment: Static Site Generator (level=story)'
created_by: xgd
created_at: '2026-06-28T20:19:49.034219+00:00'
updated_at: '2026-06-28T20:19:49.034219+00:00'
completed_at: null
last_field_updated: created_at
result: pass
fields:
  report_kind: capability_validation
  subject_uid: capability-820fbc22
  level: story
  violations: 0
  warnings: 0
  needs_review_count: 0
---

# Capability-Intent Alignment: Static Site Generator
# Level: story

**Result**: PASS
**Violations**: 0
**Warnings**: 0
**Needs review**: 0

## Cumulative Intent Considered

CAP-35 (`capability-820fbc22`) has a single story, STORY-43 (`story-d111f966`,
`story_kind=feature`), bound to a single intent: BUNDLE-2 (`bundle-94e1d1b6`), a
`free_and_reconciled` bundle of REQ-1..REQ-8 merged at commit `8ebe122`. Neither
the capability nor the story carries any `updated_by` chain, and no later intent
retires generator behavior. The relevant slice of that bundle is **REQ-6**
("tools/generate + 1st Contact marketing site definition + wire public-site").

| Intent ID | Status | When | Asked / changed (CAP-35 slice) | Counts? |
|---|---|---|---|---|
| BUNDLE-2 (REQ-6) | free_and_reconciled | 2026-06-25 (merged 8ebe122) | Build `tools/generate`: `runGenerate` API + `fc-generate` CLI; load/validate `site.json`+assets (SiteLoadError w/ JSON-pointer report); render module instances via Astro Container wrapped in id/`data-module-instance` anchors; per-site `theme.css` (theme tokens + module styles); `<head>` SEO + vetted Google Fonts links; copy assets to `<out>/assets/site/`; `--clean` wipe | YES |

REQ-6 also bundled framework additions (`fonts.ts` vetted shortlist,
`loadModuleStyles()`) and a site-schema `ContentValue` widening. Those are
**other capabilities' intent** (Framework, site-schema) which the generator only
*consumes*; STORY-43 correctly treats them as dependencies it "builds on," not as
its own surface. REQ-6's site definition (`sites/1stcontact/site.json`), the
public-site Worker delivery (ASSETS binding, HEAD delegation, plain-text 404), and
the generate-before-deploy CI/deploy ordering belong to **CAP-36 / STORY-44**
(Public Site Delivery, assessed PASS in REPORT-674) — STORY-43 lists these as
out of scope with a `[[story-public-site-worker]]` cross-reference.

## Alignment Ledger

| Element | Intents aligned to | Outcome |
|---|---|---|
| STORY-43 (`story-d111f966`) | BUNDLE-2 / REQ-6 (generator slice) | aligned — body fully and accurately expresses the `tools/generate` surface; out-of-scope boundaries to CAP-36 (delivery) and REQ-7 (lead capture) are explicit and correct; no stale/unsupported claims |

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | info | consistency | STORY-43 | — | Technical Context notes the generator conditionally emits a Turnstile `<script>` + `fc-turnstile-site-key` meta in `<head>` (when a `contact-form` module is present AND `TURNSTILE_SITE_KEY` is set). The story correctly attributes the *intent* for this to REQ-7 / `[[story-lead-capture-pipeline]]` and explicitly excludes it from this story's ACs. Behavior physically lives in generator code but is owned by another story's intent — a clean cross-capability boundary note, not drift. | none |

## Notes for the Editor

Re-stating the three-property result at story level:

- **Consistency** — every claim in the STORY-43 body maps to a behavior REQ-6
  describes (programmatic `runGenerate` result, `fc-generate` flags + non-zero
  exit, recursive load + schema validation via `SiteLoadError`, module-instance
  anchor wrapping, concatenated per-site `theme.css`, `<head>` SEO + vetted-font
  links, asset copy to `<out>/assets/site/`, `--clean`). No text references a
  retired or nonexistent intent. The `vite-node` shim and no-op build step are
  flagged in the body as implementation detail, consistent with REQ-6.
- **Coverage** — the generator surface REQ-6 calls for is fully expressed by
  STORY-43 (and decomposed across its 13 ACs, AC-443..AC-455). Generator-adjacent
  behaviors REQ-6 bundled but that belong elsewhere (framework fonts/styles,
  site-schema widening, public-site Worker, CI ordering) are correctly excluded.
- **Exclusivity** — single story in the capability; no intra-capability overlap.
  The shared `generate` touchpoint with STORY-44 is split cleanly: STORY-43 owns
  the generator's transformation logic; STORY-44 owns *invoking* it from the
  public-site app and the deploy ordering. No two elements describe the same
  intent-mandated behavior.
