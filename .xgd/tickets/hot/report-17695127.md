---
uid: report-17695127
id: REPORT-606
type: report
title: 'Capability-Intent Alignment: Builder UI (level=ac)'
created_by: xgd
created_at: '2026-06-27T00:54:17.036926+00:00'
updated_at: '2026-06-27T00:54:17.036926+00:00'
completed_at: null
last_field_updated: created_at
result: fail
fields:
  report_kind: capability_validation
  subject_uid: capability-6694c60f
  level: ac
  violations: 1
  warnings: 1
  needs_review_count: 0
---

# Capability-Intent Alignment: Builder UI
# Level: ac

**Result**: FAIL
**Violations**: 1
**Warnings**: 1
**Needs review**: 0

## Cumulative Intent Considered

Chronological ledger of intents that touched this capability.

| Intent ID | Status | When | Asked / changed | Counts? |
|---|---|---|---|---|
| BUNDLE-2 (bundle-94e1d1b6) | free_and_reconciled | created 2026-06-15, merged 2026-06-25 (commit 8ebe122e) | Bundles REQ-1..REQ-5 + 3 more; delivered the Phase 0 chat-driven Builder UI: two-panel SPA shell, collapse/restore rail, draggable splitter, viewport presets, four-layer AI tool validation, localStorage persistence, `/api/chat` Anthropic proxy, `/builder` static-asset routing, starter-site bootstrap | YES |

Note: this is an `ac`-level check. Per the level cascade, the story body (STORY-46) is the working reference and is treated as already aligned to intent by the prior story-level cycle. STORY-46's body is internally consistent and traces cleanly to BUNDLE-2; no ambiguity forced escalation to intent history.

## Alignment Ledger

Per-AC alignment to the STORY-46 body (story-ba9f2715, story_kind=feature → ACs expected).

| Element | Aligned to (story in-scope item) | Outcome |
|---|---|---|
| AC-477 | `/builder` + `/builder/` → static-asset shell; other paths served unchanged | aligned |
| AC-478 | chat collapse → 32px rail + restore-to-width; persist collapsed flag + width | aligned |
| AC-479 | restore rail on LEFT edge (DOM order chat→splitter→rail→preview) | aligned |
| AC-480 | splitter drag resizes chat, clamps min/max, persists final width | aligned |
| AC-481 | viewport presets mobile 375 / tablet 768 / desktop 100%, single active, desktop default | aligned |
| AC-482 | preview iframe fills panel height, not 150px default | aligned |
| AC-483 | accepted tool call advances site + re-renders preview + `accepted:true` log | aligned |
| AC-484 | rejected tool call unchanged + structured error (dial/value/enum) in log | aligned |
| AC-485 | working site definition persisted, survives re-mount, >1MB warning | aligned |
| AC-486 | `/api/chat` proxies Anthropic; 8-tool surface by name; catalog system prompt; `{text,toolCalls}` | aligned |
| AC-487 | `/api/chat` 500 on unbound key, 502 on upstream throw/non-2xx | aligned |
| STORY-46 in-scope: chat-turn history persistence | — | gap: no AC covers it (see Finding 1) |
| STORY-46 in-scope: starter-site fetch at boot + `?site=` selection | partially AC-477 (serving side) | gap: client fetch/selection not covered (see Finding 2) |

## Findings

| # | Severity | Property | Element | Resolution category | Issue | Suggested edit |
|---|---|---|---|---|---|---|
| 1 | violation | coverage | STORY-46 (story-ba9f2715) AC tree | ac-add | STORY-46 in-scope explicitly states: "localStorage persistence of the working site definition **and the chat-turn history**; on reboot, a previously-edited site survives **and chat history is preserved across store re-instantiation**." AC-485 covers only the working-site-definition persistence; no AC covers chat-turn history persistence. The capability body's "persistence of working state to localStorage" promise is therefore only half-evidenced. | Author an AC asserting that chat-turn history is persisted to browser storage on each turn and is restored when the builder re-mounts against the same storage (parallel to AC-485 but for the chat log). |
| 2 | warning | coverage | STORY-46 (story-ba9f2715) AC tree | ac-add | STORY-46 in-scope states: "A starter site (`/starter-sites/1stcontact.json`) is fetched at SPA boot from the same origin; the `?site=` query param selects which starter to load (defaults to `1stcontact`)." AC-477 covers the worker *serving* the starter JSON, and AC-483/484/485 *mount against* a starter that is supplied directly in the test — but no AC covers the SPA *fetching* the starter at boot or the `?site=` selection/default behavior. | Author an AC asserting that on SPA boot the starter site is fetched from same-origin `/starter-sites/<name>.json`, that `?site=<name>` selects the named starter, and that the default is `1stcontact`. |

## Notes for the Editor

- Consistency: clean. Every AC (AC-477..AC-487) traces to an in-scope behavior in STORY-46 and is internally consistent with the story body. No AC references out-of-scope behavior (D1 save/load, auth gating, streaming, the deferred DOC-8 §5.1 tool half, diff/click-to-edit). AC-484's `size: 'huge'` example correctly matches the story's documented DOC-8 §5.3 divergence note.
- Exclusivity: clean. AC-478 (collapse-triggered persist) and AC-480 (drag-triggered persist) both touch chat-width persistence but verify distinct triggers/behaviors — not duplicates. AC-481/482, AC-483/484, AC-486/487 are complementary pairs (preset vs height; accepted vs rejected; happy vs error), not redundant.
- The two coverage gaps are the same shape (a persistence/bootstrap behavior listed in the story's in-scope description but absent from the AC tree). Finding 1 is graded violation because it is a distinct data-persistence guarantee whose failure means silent loss of user conversation state; Finding 2 is graded warning because the serving half is covered by AC-477 and the starter is exercised indirectly by AC-483/484/485, leaving only the boot-fetch/`?site=` selection path unevidenced.
- Both gaps resolve by adding ACs (`ac-add`); no AC requires deprecation or edit, and no story-body edit is needed — the story body already describes both behaviors correctly. The drift is matrix under-expression at the AC layer, not a story/intent mismatch.

