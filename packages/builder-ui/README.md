# @1stcontact/builder-ui

The chat-left / live-preview-right builder SPA.

Design discussion: CHAT-9 (Builder). Reuses the XGD chat/list panel collapse-to-vertical-bar pattern with a draggable splitter between chat and preview.

Served by `apps/control-app` as static assets at `app.1stcontact.io/builder`.

> Not yet implemented — placeholder for the package skeleton.
