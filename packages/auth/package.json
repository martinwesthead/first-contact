{
  "name": "@gendev/auth",
  "version": "0.0.0",
  "private": true,
  "description": "Headless magic-link authentication library: token issuance, session model, access+refresh exchange, Bearer-token wrapper. No UI.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
