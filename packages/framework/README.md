# @gendev/framework

Section library, renderer, and theme-token system.

Consumed by:

- `tools/generate` — static generator (build-time)
- `packages/builder-ui` — in-browser builder preview (CHAT-9)
- `apps/public-site` — at request time for any dynamic fragments

Design discussion: CHAT-7 (Framework).

> Not yet implemented — placeholder for the package skeleton.
