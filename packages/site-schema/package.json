{
  "name": "@1stcontact/site-schema",
  "version": "0.0.0",
  "private": true,
  "description": "JSON schema + TS types for site definitions — shared contract between AI, builder UI, and static generator",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "echo 'site-schema: no source yet'"
  }
}
