# @gendev/site-schema

JSON schema + TypeScript types for site definitions.

The contract enforced at every boundary:

- AI orchestration emits diffs that conform to this schema
- Builder UI state validates against this schema
- Static generator consumes this schema

> Not yet implemented — placeholder for the package skeleton.
