{
  "name": "@gendev/web-fetch-safety",
  "version": "0.0.0",
  "private": true,
  "description": "Shared safety layer for every external-URL fetch path: SSRF / scheme / size / redirect guards, KV-backed response + robots cache, and the contract every fetch tool must go through.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "dist"
  ],
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260617.1",
    "typescript": "^5.6.0"
  }
}
